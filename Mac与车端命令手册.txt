XT-STCAR：Mac 与 Muse Pi Pro 车端命令手册
维护日期：2026-09-15
适用工程：/Users/yuhaojin/Documents/XT-STCAR

本文件是操作用法，不表示这些命令已经在车端执行。当前暂不接车。
Mac 已完成的测试、交叉构建和模型验证，以 README、agent.md 和最新验证记录为准。
车端的设备身份、系统版本、推理运行库、标定、实际停车距离和速度都仍需实测。

使用约定
  [Mac] 在开发机终端执行，通常先进入工程根目录。
  [车端] 以后取得新车连接信息、登录新车后执行；不是在 Mac 执行。
  每组命令都说明用途、替换项和预期结果。不要整篇一次粘贴执行。
  路径含空格或中文时保留双引号。反斜杠表示命令在下一行继续。
  “替换为...”和示例账号、IP、文件名均需按实际情况修改。
  192.0.2.10 仅为文档示例 IP，不是已知车辆地址；不沿用旧车账号或地址。
  本工程没有可执行的底盘带动力发送入口。回放、仿真与 PWM 预览不会控制电机。
  serial-capture --execute 是单个传感器的串口读取入口，不发送设备命令。
  Mac 工作目录是完整源码仓库，使用 cargo、scripts/build-riscv.sh、打包和上传脚本。
  车端工作目录是单次解包的 release，使用 bin/xt-stcar 和 bin/xt-stcar-robot。
  部署包没有完整 Cargo 源码，不在 release 里运行 cargo build、git pull 或 Mac 工具链。

一、Mac：进入工程与检查环境

1.1 进入项目
[Mac；用途] 确认当前路径，使用项目锁定的 Rust，而非改变全局默认工具链。
[替换项] 本机无需替换；队友使用自己的真实工程目录。

  cd /Users/yuhaojin/Documents/XT-STCAR
  pwd
  uname -m
  sw_vers
  rustc --version
  cargo --version
  rustup show active-toolchain
  rustup target list --installed

[预期] 本机 uname 为 arm64，Rust/Cargo 为 1.97.1；已装目标含
  riscv64gc-unknown-linux-gnu。Mac 主机目标为 aarch64-apple-darwin。
  Mac 的 Mach-O ARM64、旧 ARM64 Linux 与新车 RISC-V Linux 二进制不能互换。
  Mac 不运行 GLIBC；GLIBC 2.38 是 Linux 目标的交叉链接基线。

1.2 检查工程内交叉编译工具
[Mac；用途] 核对已有工具，不重复安装。
[替换项] 无，均相对工程根目录。

  toolchains/zig-aarch64-macos-0.15.2/zig version
  toolchains/cargo-zigbuild-0.23.4/cargo-zigbuild --version
  cat rust-toolchain.toml

[预期] Zig 0.15.2、cargo-zigbuild 0.23.4；缺文件时先恢复项目环境，
  不从另一架构复制可执行工具，也不把工具链上传给车端。

1.3 Python 仅用于模型工具和交付脚本
[Mac；用途] 检查已有独立环境。Rust 默认推理不需要激活 Python 环境。
[替换项] 无。

  .venv/bin/python --version
  .venv-model/bin/python --version
  .venv-model/bin/python -m pip check

[预期] 模型环境依赖检查通过。.venv 与 .venv-model 分开，不混装。
  后文明确使用 .venv-model/bin/python，无需 source，也不修改系统 Python。

二、Mac：日常检查、构建与 GLIBC 2.38

2.1 本机质量检查
[Mac；用途] 格式、完整 workspace 测试、静态检查、本机两程序构建。
[替换项] 无。

  cargo fmt --all --check
  cargo test --workspace --locked
  cargo clippy --workspace --all-targets --locked -- -D warnings
  cargo build --workspace --locked
  target/debug/xt-stcar --help
  target/debug/xt-stcar-robot --help

[预期] 检查退出码 0；两个程序帮助与下面用法一致。
  fmt 检查失败时，可执行 cargo fmt --all 格式化，再审阅修改。
  --locked 保持 Cargo.lock；只有依赖已缓存时才附加 --offline。
  部分真实模型测试需显式准备模型/运行库，普通测试不等于真实相机或实车测试。

2.2 单模块检查
[Mac；用途] 修改局部后先验证相关模块，完整交付前仍执行整套检查。
[替换项] -p 后换成实际修改的 crate；测试名须为实际文件名。

  cargo test --locked -p xt-stcar-robot-core --test mission
  cargo test --locked -p xt-stcar-robot-runner --test telemetry
  cargo test --locked -p xt-stcar-device-io

[预期] 各自测试通过。device-io 的 PTY 测试使用主机伪终端，不打开真实车辆设备。

2.3 RISC-V release 交叉构建
[Mac；用途] 用工程脚本执行 fmt/test/clippy、交叉链接及两个 ELF 核验。
[替换项] 无；依赖未缓存时去掉 --offline，允许 Cargo 获取锁定依赖。

  scripts/build-riscv.sh --help
  scripts/build-riscv.sh --offline

[预期] 目标 riscv64gc-unknown-linux-gnu.2.38；产物位于：
  target/riscv64gc-unknown-linux-gnu/release/xt-stcar
  target/riscv64gc-unknown-linux-gnu/release/xt-stcar-robot
  同目录 *.elf.json 和 xt-stcar.build.json 记录 ABI、依赖、源码及二进制哈希。
  这里没有升级、安装或覆盖车端 libc。实际最大 GLIBC 引用看当前 ELF 报告，
  不把“允许上限 2.38”当作“所有二进制恰好需要 2.38”。

2.4 复核交叉产物
[Mac；用途] 静态检查架构、动态加载器、GLIBC 上限和哈希。
[替换项] 无。inspect_elf.py 不执行目标程序。

  file target/riscv64gc-unknown-linux-gnu/release/xt-stcar
  file target/riscv64gc-unknown-linux-gnu/release/xt-stcar-robot
  python3 scripts/inspect_elf.py target/riscv64gc-unknown-linux-gnu/release/xt-stcar
  python3 scripts/inspect_elf.py target/riscv64gc-unknown-linux-gnu/release/xt-stcar-robot
  shasum -a 256 target/riscv64gc-unknown-linux-gnu/release/xt-stcar
  shasum -a 256 target/riscv64gc-unknown-linux-gnu/release/xt-stcar-robot

[预期] ELF64、RISC-V、RVC/LP64D、PIE，加载器
  /lib/ld-linux-riscv64-lp64d.so.1；检查 JSON 中 passed 为 true。
  needed 列表与 maximum_glibc 是当前结果；还须在新车核对对应依赖。
  Mac 使用 shasum -a 256；车端 Linux 通常使用 sha256sum。

三、Mac：视觉自检、预处理、张量回放与真实模型

以下命令使用本机构建的 target/debug 程序；也可用
  cargo run --locked --bin xt-stcar -- 后接相同参数。

3.1 无模型自检
[Mac；用途] 用合成图片/检测张量验证 Rust 视觉处理链。
[替换项] 无。

  target/debug/xt-stcar self-check --config config/yolo26n.json

[预期] JSON 自检报告；不加载 ONNX Runtime、不执行真实模型、不打开相机。

3.2 预处理实际图片
[Mac；用途] PNG/JPEG 转为 letterbox 后的 FP32 NCHW 输入及坐标变换文件。
[替换项] IMAGE 改为已存在图片绝对路径；输出名选新的诊断文件。

  mkdir -p work/commands
  IMAGE='/替换为实际路径/image.png'
  target/debug/xt-stcar preprocess --image "$IMAGE" \
    --tensor work/commands/input.f32 --transform work/commands/letterbox.json \
    --config config/yolo26n.json

[预期] 两个文件：小端连续 float32 张量与 letterbox JSON。
  此 input.f32 是模型输入，不是下一项 replay 要求的检测输出 JSON。

3.3 已保存的模型输出张量回放
[Mac；用途] 单独检查 YOLO 后处理，不执行推理。
[替换项] IMAGE 同上；OUTPUT_TENSOR 指向真实输出 JSON，结构为
  {"shape":[1,N,6],"values":[...]}，每行是 x1,y1,x2,y2,score,class_id；
  当前锁定 YOLO26n 的 N=300，具体契约以 config/yolo26n.json 为准。

  OUTPUT_TENSOR='/替换为实际路径/detection-output.json'
  target/debug/xt-stcar replay --image "$IMAGE" --tensor "$OUTPUT_TENSOR" \
    --config config/yolo26n.json --output work/commands/tensor-replay.json

[预期] 检测框、类别、置信度等 JSON；不接受上项的二进制 input.f32。

3.4 Rust 原生 YOLO26n 推理
[Mac；用途] 实际读取图片、ONNX 和 Mac ORT C 库执行推理。
[替换项] IMAGE；模型、provenance 与当前锁定接口应匹配。

  target/debug/xt-stcar infer --image "$IMAGE" \
    --model models/yolo26n.onnx \
    --runtime-lib toolchains/onnxruntime-macos-arm64-1.29.0/lib/libonnxruntime.1.29.0.dylib \
    --config config/yolo26n.json --timeout-secs 30 \
    --output work/commands/detections.json

[预期] JSON 检测结果；模型旁自动读取 models/yolo26n.provenance.json。
  这是 Mac 推理结果，不能证明 RISC-V ORT/厂商 EP 或车端帧率。
  --timeout-secs 的原生超时请求协作取消，不能视作硬实时抢占。

3.5 可选 Python 参考对照及模型静态校验
[Mac；用途] 仅供离线对照，不改变默认 Rust 后端。
[替换项] IMAGE；使用已经存在的模型环境。

  target/debug/xt-stcar infer --backend python-reference --image "$IMAGE" \
    --model models/yolo26n.onnx --python .venv-model/bin/python \
    --config config/yolo26n.json --output work/commands/reference.json
  .venv-model/bin/python scripts/validate_yolo26.py \
    --model models/yolo26n.onnx --spec config/yolo26n.json

[预期] 参考检测 JSON、模型校验 JSON。模型导出属于另外一次明确操作，
  如需重导出先读 docs/YOLO26接口.md，并用
  .venv-model/bin/python scripts/export_yolo26.py --help 查看参数，
  保留原权重、原 ONNX 及来源记录；不要为普通运行重复导出或混装依赖。

四、Mac：安全状态机回放、协议与 PWM 预览

4.1 旧格式事件回放
[Mac；用途] 验证安全状态机、超时、急停及协议解析。
[替换项] 无；输出路径可选新名字。这里的输入带 event 字段，可含人工 Motion。

  target/debug/xt-stcar-robot replay --events examples/robot-sim.jsonl \
    --config config/robot-sim.json --output work/commands/robot-replay.jsonl
  target/debug/xt-stcar-robot replay --events examples/robot-imu-replay.jsonl \
    --config config/robot-imu-replay.json --imu-config config/imu-replay.json \
    --output work/commands/imu-replay.jsonl
  target/debug/xt-stcar-robot replay --events examples/robot-n10-replay.jsonl \
    --config config/robot-n10-replay.json --n10-config config/n10-replay.json \
    --chassis-calibration config/chassis-calibration-sim.json \
    --output work/commands/n10-replay.jsonl

[预期] step + summary JSONL，physical_output_enabled=false，结束有 Stop。
  IMU/N10 合成样例故意包含过期/坏数据，末尾 Fault 可以是预期结果。
  replay 原入口不会自主生成比赛路线；视觉输入事件可配
  --model、--runtime-lib、--vision-config，三者用法见程序帮助。
  单个 N10 packet 的 16 束局部样本，不等于 autonomy 所需全圈扫描。

4.2 厂商话题语义的 7 字节帧预览
[Mac；用途] 检查编码，不打开底盘串口。
[替换项] 无；仅示例，不是标定值。

  target/debug/xt-stcar-robot chassis-preview \
    --profile teleop_pwm_degrees --linear 1500 --angular 90

[预期] 中性 PWM 与编码字节 JSON。还支持 navigation1300 和
  navigation_one1200，但其 linear/angular 沿用厂商原话题约定。
  不把 1200/1300 系数当作 m/s 到 PWM 的实车标定。
  物理速度/曲率预览用 replay --chassis-calibration；表外值拒绝外推，
  映射错误保留 Stop/急停诊断并非零退出；预览没有物理输出。

4.3 串口采集计划
[Mac；用途] 离线查看预计打开的设备、波特率、时长和字节限额。
[替换项] 无，厂商路径仅为参考；不要在 Mac 对这些参考路径加 --execute。

  target/debug/xt-stcar-robot serial-capture \
    --config config/serial-n10-capture.json --output work/commands/n10-capture.jsonl
  target/debug/xt-stcar-robot serial-capture \
    --config config/serial-imu-capture.json --output work/commands/imu-capture.jsonl

[预期] 计划 JSON；没有 --execute 时不打开设备，也不生成所列采集文件。
  已有配置参考：N10 /dev/laser 230400、WIT IMU /dev/imu 115200；
  真实设备必须以后按身份核实，不能把 /dev/car 作为传感器采集口。

五、Mac：自主比赛离线闭环、道路识别和传感器快照回放

路径跟踪 A/B（2026-09-09 新增）
[Mac；用途] 在直线/弯道及合成转向响应下比较 Pure Pursuit 与实验 LQR。
[替换项] 无。输出一份 JSON 到 stdout；不读写设备。第一条只测跟踪层，第二条执行完整比赛状态机。

  cargo run --release --locked --offline -p xt-stcar-robot-core --example tracking_comparison
  cargo run --release --locked --offline -p xt-stcar-robot-runner --example motion_comparison

[预期] 两算法的横向/航向误差及失败指标；这是跟踪层合成实验，
  不代表车端帧率。第二条在同一完整模拟比赛中比较两算法，并保留失败摘要；
  它正常退出不表示两算法都完成比赛，要看各自 summary.completed/fault。
  LQR仍为实验选项，默认Pure Pursuit保持不变；固定布局历史见docs/赛场规格自适应.md，
  本轮在线模式见第十二节及docs/在线元素驱动任务.md；各算法须分别看completed和fault。
  docs/motion-v6-competition-comparison.json及motion-v5/v4/v3/v2、motion-control均保留历史，
  不当作当前源码的验收结果。
  summary.first_navigation_failure 为首个非预期阻塞/故障窗口，最多8帧前文、
  触发帧、8帧后文；null表示没有触发。先看触发帧的navigation_reason、
  navigation.candidates及请求/选中曲率，再对照最终fault；首次阻塞可能恢复。
  该字段在内存保留，结束随摘要写出，无需默认开启--trace。
  目前autonomy-sim和完整比赛A/B有此窗口，autonomy-replay尚未接入。
  常规完整比赛仍运行下方 autonomy-sim。
  原有模拟配置未写 navigation.tracking 时默认 Pure Pursuit；LQR 配置
  及模型边界见 docs/运动控制设计与对比.md。不要直接用于实车。

路径连续性、采用时基与性能门（v9，Mac离线）
[Mac；用途] 检查源历史/采用约束、停稳后恢复、真实后台Worker和最终输出采用。
[替换项] 无；这些是纯主机测试，不能用于启动电机。模块位置如下：
  crates/robot-core/src/admission.rs：共享停车矩形、当前采用区间和有限多拍前瞻。
  crates/robot-core/src/admission/slew.rs：共享真实命令变化时基，校验修订/启动状态；预测时另跟踪模型变化时刻。
  crates/robot-core/src/navigation.rs：基础rollout通过后才做后备前瞻；route_rebuild_reason记录首个缓存失效原因。
  crates/robot-core/src/navigation/continuity.rs：预测状态下的数值初猜接续、恢复连接族和同姿态剩余长度诊断。
    fixtures/lqr45360_terminal_seed_origin.json为原始窗口；lqr45180_continued_family.json为明确标注的数值实验。
  crates/runner/src/control_admission.rs：从同一源、实际last-change/revision和lease准备约束；无效值明确失败。
  crates/runner/examples/async_timing_matrix.rs：专用2×2矩阵与有界内存分叉对比；默认控制不记录这份轨迹。
  crates/runner/examples/support/performance_gate.rs：模拟结束后的性能验收，不参与控制；预算在examples/fixtures/motion-v9-performance-budget.json。
  crates/runner/src/control_execution.rs：最终独立采用证书和固定容量真实命令历史。
  crates/runner/src/control_execution/diagnostics.rs：结构化证书首因、约束索引、符号余量及时间窗。
  crates/robot-core/src/navigation/recovery.rs：普通/恢复前向搜索诊断和连续几何恢复。
  crates/robot-core/src/navigation/primitive_envelope.rs：渐变速度/曲率积分的数值误差界与沿路传递。

  cd /Users/yuhaojin/Documents/XT-STCAR
  cargo test --locked --offline -p xt-stcar-robot-core --lib admission::tests
    当前高V/K不能被新低速命令删除；两拍墙面陷阱须提前减速，迟采用保留其源到规划之间的旧命令。
  cargo test --locked --offline -p xt-stcar-robot-runner --lib control_admission::tests
    准备约束保留源年龄/采用窗/原期限，NaN历史、错源和过期必须明确拒绝。
  cargo test --locked --offline -p xt-stcar-robot-runner --lib control_execution::tests
    检查最终独立证书和结构化失败；不把前置模型预测当作输出采用凭证。
  cargo test --locked --offline -p xt-stcar-robot-core --lib navigation::recovery
    完整停稳源快照、网格首层拒绝、连续几何恢复和新障碍回归。
  cargo test --locked --offline -p xt-stcar-robot-core --lib navigation::primitive_envelope
    渐变与饱和段的积分误差、镜像、误差传播和无效数值回归。

  cargo test --locked --offline -p xt-stcar-robot-core --test terminal_approach
    检查21/41/61/81档与左右镜像、新障碍拒绝旧初值、普通最佳优先和共享预算。
  cargo test --locked --offline -p xt-stcar-robot-runner --test async_deadlines
    真实Worker阶段挂起，逻辑时钟和输出/车辆仍推进；覆盖采用窗、原lease、替换、revision和转向变化率。
  cargo test --locked --offline -p xt-stcar-robot-runner --test async_competition
    完整异步框架的断流/故障停车与有界事件时序检查。
  cargo run --release --locked --offline -p xt-stcar-robot-runner --example async_motion_comparison -- both
    同一整场分别跑PP/LQR，默认真实源延迟60/80ms、采用偏移3/7/9ms，plant只用poll.command。
    任一失败仍输出完整JSON，然后返回非零；检查本次completed/fault，不要忽略退出码或套用旧版本结论。
    可用参数pp或lqr单独运行。功能钟等后台时暂停；模拟完成不证明目标计算满足期限。
  cargo run --release --locked --offline -p xt-stcar-robot-runner --example async_host_clock
    约3秒真实Mac时钟短转弯/断流观察；不等待worker来冻结时钟，保留排队/导航/证书/采用耗时。
    短场景把斑马线放到当前不可见处以观察搜索接近时的转向，不是更改完整比赛配置。
    默认不打开设备，末尾才输出JSON；需要保存可在命令末尾加 > work/自己的报告.json。
  cargo run --release --locked --offline -p xt-stcar-robot-runner --example motion_comparison
    同步与异步工况分别报告；两者均经过完整任务状态机，不能混用完成时间或计时口径。

  固定2×2时序矩阵（PP/LQR共8场；无额外命令行参数）：

  mkdir -p work
  cargo run --release --locked --offline -p xt-stcar-robot-runner --example async_timing_matrix > work/async-timing-matrix.json 2> work/async-timing-matrix.stderr.log
  echo $?

  [预期] JSON中runs保留每场完整summary，single_factor_comparisons一次只改变输入延迟或采用偏移。
    实际输出按共同模型时间比较；规划动作/路径点列/路线revision/点数/恢复按同一source_at比较。
    点列精确不同不证明路线拓扑改变。查看all_completed、traces_complete、performance_gate.all_passed和每场fault；失败、截断或性能超限均非零退出。
    v9预算在试改前固定：相同时序v7/v8中较快已完成场的整场时间/距离110%，阶段加max(1000ms,10%)、实际Stop加1000ms。
    性能门只检查离线结果，绝不作为提高车速、放宽碰撞/比赛门限或增加搜索预算的依据。失败项见performance_gate.runs[].failures。
    output_trace_counts.stop_duration_ms是实际Stop命令持续时长，含启动/最终制动，不是车辆物理静止时长。
    stop_episodes_including_startup_and_shutdown与summary中导航非预期Stop分别统计；
    drive_during_rejection_polls表示拒绝期间仍保持旧Drive，不能把拒绝次数全算成停车。
    每场内存上限16384次命令变化、2048份计划、每计划2048路径点，超额有omitted计数；
    只有traces_complete=true时空差异才表示共同观察区间未见差异。结束统一写JSON，不逐拍写eMMC。
    simulate_async_observed仅离线只读回调，观察耗时计入host wall_time_ms；功能钟等后台时仍冻结，不能当成WCET。

  完整异步example可在跟踪器参数后传入一个本地JSON时序文件。默认配置示例：
  {"capture_period_ms":100,"output_period_ms":20,"input_delays_ms":[60,80],"adoption_delays_ms":[3,7,9],"stop_input_at_ms":null,"measure_wall_time":false}
  显式measure_wall_time=true才读主机时钟并累计有界耗时统计，不修改source/planned/lease或控制判定规则；采样开销仍可能影响主机调度。
  不用planned_at冒充worker实际开始时刻；排队、计算、发布到观察分别看。
  另一组复验时序把input_delays_ms改为[40,60]、adoption_delays_ms改为[5,9,11]，
  其他字段保持；将JSON存为新的work文件，再传到both参数之后。每次运行保存自己的stdout/stderr和退出码。

  阅读诊断时先分清三处：
    navigation.diagnostics.candidates.admission_current / admission_next 是候选筛选拒绝次数；
    observed_plan.certificate_failure 是最终独立证书的首个失败，可能为null；
    poll.command才是实际输出，Stop次数不等于候选拒绝次数，证书拒绝为0也不等于没有停车。
  certificate_failure.reason区分时间/输入、速度/转向/侧向、地图、激光点、视觉锥桶和灯前边界；
  constraint_index从0开始，signed_margin配margin_unit阅读，负值为超限，障碍相切为0也拒绝。
  无法定义的数值余量留空；source_at/pose_at/oldest_sensor_at/planned_at、lease_expires_at、
  adoption_through和历史/最终V/K用于对照同一源，不得改时间戳或删除旧高速历史来缩小矩形。
  navigation.diagnostics.forward_search分开普通与恢复的节点数、primitive尝试/首个失败和退出原因；
  no_forward_kinematic_path只表示这次有界搜索未找到，不是数学不可达。

  v8时基：planned_at、last_command_change_at、adopted_revision及持有目标来自同一真实历史。
  候选在规划窗起点（最早采用时刻）按实际last-change确定曲率区间；前置与证书同定义，poll按实际当前时刻复核。
  同一命令重复输出不刷新时基，仅速度变化仍刷新；Stop归零目标后渐变回中，恢复使用Stop真实采用时刻。
  只有revision=0且确无命令变化的启动状态沿用一周期起步限幅；历史缺失、未来时间、矛盾修订均明确拒绝。
  不在poll裁剪后直接发出不同的未经认证命令，不延长原lease，不清空当前源的高速/高曲率历史。
  轻量当前采用检查→原运动/停车/Grid/足迹rollout→多源前瞻→终端认证与评分；rollout只积分一次，原门限全部保留。
  短路首因和工作计数可能改变，不能直接当成动作差异或CPU加速。
  route_rebuild_reason记录初始/任务停车、arrival/边界/目标变化、车体参考偏离、跟踪拒绝和无候选等首个重建触发；
  搜索未成功时保留原因，缓存复用时为空，不声称它是整场失败的唯一根因。

  前置前瞻取最早/中间/最晚3种采用时刻，下一采集相位保留当前源年龄，预测正常减速后备过程。
  每样本最多64拍/2秒、固定65个源槽；admission_forecast_work中的source_checks/projection_calls/
  projection_intervals记录实际请求的工作量。projection_intervals是预扣1ms模型区间，不是CPU指令数。
  未来源只累计采集至规划之间的预测命令；当前源真实历史不变。未来延迟按当前源年龄建模，
  这3种有限模型样本不能证明连续全采用窗或未来路线必可行；最终证书/poll仍独立验证。
  ForecastBudget表示受本次前瞻额度限制而未完成检查，不能降低安全条件继续放行。
  连续域另预留最小正常起步候选所需的制动空间，避免静态可停却无法再次正常起步。
  连续恢复使用完整车体外接圆、间隙、障碍半径与积分误差界，不靠倒车、原地旋转或放宽比赛容差。
  普通与恢复共享原搜索/终端额度；调试计数与首因在内存保留，默认无逐tick写盘，不降比赛频率。

  新增导航/阶段诊断JSON省略全默认对象与None；缺失表示零工作/未搜索/无约束或失败，有值完整保留。
  以下为v9历史结果，说明见docs/路径连续性与性能回归.md，报告为docs/motion-v9-*。
  固定布局历史见第十一节及docs/field-adaptation-*；在线模式见第十二节和docs/online-mission-*。
  v9的8/8交叉工况完成并通过试改前固定的性能门，最终证书/曲率slew采用拒绝和终端总预算耗尽0。
  固定顺序为 [60,80]×[3,7,9]、[60,80]×[5,9,11]、[40,60]×[3,7,9]、[40,60]×[5,9,11]：
  PP75.467/54.985/56.443/54.449秒；LQR54.663/55.265/54.249/53.445秒。
  早输入/早采用LQR从v8的88.647s/16.515558m到54.249s/10.291006m，实际Stop4513ms保持。
  PP四组时间/路程和同步PP58.8秒/LQR54.3秒保持；不代表任意时序或实车都收敛。
  工作区371通过/2忽略、跟踪example2/矩阵example5、交付35通过，fmt/clippy/双RISC-V静态链接通过。
  v9有108份编译输入、GLIBC2.38基线/实际最高2.34；历史binary和包哈希见motion-v9-build/validation/delivery。
  默认异步主机导航平均/最大PP6.216/59.705ms、LQR4.346/11.099ms，不是目标WCET；短场23源、poll最大21ms，
  末源2200、原期限2450、2460ms Stop、2686ms模型停稳回中。暂不接车，未执行目标程序。

  cargo test --locked --offline -p xt-stcar-robot-core --lib navigation::continuity::tests
    7项：原始120ms窗口及镜像、旧history一致性/记录投影状态Navigator重放、同姿态旧/新长度、
    提前/负向/无效/耗尽位移、新障碍/边界/共享额度、恢复族接续、生命周期清理。
    不是独立重跑全部worker历史重建；真实worker全链路由矩阵和runner测试覆盖。
  cargo test --locked --offline -p xt-stcar-robot-runner --example async_timing_matrix
    5项：实际Stop/对齐观察与性能门；门在仿真后验收，不给控制器放宽安全的入口。
  .venv/bin/python scripts/review-motion-ablation.py --output work/my-v9-ablation
    原7d、仅调整检查顺序、a623三变体独立源码和Cargo target，离线release编译并执行主机例程。
    输出目录必须不存在且位于work；不覆盖旧结果，不fetch、不接车、不执行RISC-V。
    加 --prepare-only 只验证固定提交/补丁/观察器并解包，不编译运行。若缺少历史commit或依赖，先按上传/环境说明补齐。
    原版与顺序-only控制285次变化逐值相同，54.447s/10.317616m；前瞻预扣工作减少2.62%，不是CPU比例。
  route_length_change只在同目标、旧缓存可用的footprint drift重建成功时记录旧剩余/新米数；字段缺失不代表没有绕路。
  恢复族内候选优先级会改变，所有原认证门、节点/终端额度、Q/R和真实采用时基保持。

  以下v8修复前基线为历史：
  修改前7d28761隔离基线8场7完成/1失败，详见docs/motion-v8-before.json及motion-v8-baseline-matrix.json。
  新交叉input[60,80]×adopt[5,9,11]：PP114.591s/21.435316m完成；LQR91.669s/14.627098m因普通停车超过10秒失败。
  另一交叉input[40,60]×adopt[3,7,9]：PP87.043s/16.472032m、LQR54.447s/10.317616m均完成。
  原默认/扰动4场逐值复现v7（比较时去掉主机墙钟字段）；离线observer的8场功能summary也逐值保持、无trace截断。
  以下v7是历史结果，不能当作当前源码验收：同步PP58.8s/LQR54.3s保持；默认异步PP73.863s/LQR89.069s，扰动PP56.849s/LQR53.849s均完成。
  四场最终证书拒绝0，曲率slew拒绝默认每tracker5次、扰动2次；不能当成没有实际Stop。
  Rust350通过/0失败/2忽略、example2通过、交付35通过；fmt/clippy/双RISC-V链接通过。
  98份源码/清单/嵌入fixture哈希见docs/motion-v7-build.json，GLIBC基线2.38、最高实际引用2.34。
  顺序主机同步profile PP均值1609.081→1621.487ms、LQR1494.640→1484.827ms；每种各预热1次/测3次。
  持续时钟poll最大间隔26ms，末源2201ms到期2451ms、2460ms输出Stop、2687ms停稳回中。
  完整验证/主机计时/本地包分别见docs/motion-v7-validation.json、motion-v7-host-profile.json、motion-v7-delivery.json。
  默认时序PP比v6慢16.2秒，仍需优化路线和实车计时；PP默认、LQR实验、安全/比赛期限保持。
  历史v6：同步PP58.8s/LQR54.3s；默认异步PP57.663s完成、LQR29.463s停滞失败。
  这些旧结果见docs/恢复预算与完整异步验证.md及motion-v6-*，仅供前后比较。

运动执行、终端接续与异步时间检查（v5）
[Mac；用途] 检查采用命令历史、连续渐变的侧向加速度峰值、通过点及车辆模型。
[替换项] 无。以下为 Rust 主机测试，不访问车辆设备，不启动电机。

  cargo test --locked --offline -p xt-stcar-robot-core --test steering_execution
  cargo test --locked --offline -p xt-stcar-robot-core --test through_waypoints
  cargo test --locked --offline -p xt-stcar-robot-core motion_transition::
  cargo test --locked --offline -p xt-stcar-robot-runner --test control_runtime
  cargo test --locked --offline -p xt-stcar-robot-runner --test async_autonomy
  cargo test --locked --offline -p xt-stcar-robot-runner --test through_handoff
  cargo test --locked --offline -p xt-stcar-robot-core --test terminal_approach
  cargo test --locked --offline -p xt-stcar-robot-runner --lib phase_statistics
  cargo test --locked --offline -p xt-stcar-robot-runner --lib stop_tests

[预期] 对应专项测试通过；整体验收还要运行完整比赛和 scripts/build-riscv.sh。
  MotionTransition 在 crates/robot-core/src/motion_transition.rs，解析检查速度与曲率
  同时渐变过程的 v²|曲率| 峰值；不能把单一目标值或一个采样点当作整个过渡。
  SteeringEstimate 在 crates/robot-core/src/navigation.rs，保存已采用曲率目标和
  模型执行曲率。Stop 目标为零，模型仍按变化率回中，不代表有实际舵机反馈。
  必停点使用 Stop，锥桶中间点使用 PassThrough；经过检查的尾段只提供制动余量，
  第一段仍单独跟踪，不能用前视跳过必经点；admission_radius_m从任务明确传入，
  区分原配置任务0.065m与导航停车0.045m；next_max_speed_mps提前约束
  下一阶段的候选目标速度，避免进入验收半径后才突然限速；不保证入区瞬间测量速度
  必定达标，外部超速/测量偏差仍可能触发空速度区间Stop。查看 navigation.diagnostics
  的 continuation_checked、checked_continuation_distance_m、goal_speed_cap_mps、
  execution_state 和 candidates。新车辆模型按不超过1ms子步积分并逐子步检查碰撞/边界，
  这不会提高磁盘日志频率，也不是1ms实车控制周期。
  带朝向目标的缓存参考以完整车体偏离检查，包含航向造成的角点位移；整段评分比较对应四个
  车身角的世界位置差最大值，统一以米衡量位姿误差。不能仅凭旧路径无碰撞就认为仍可到达。
  v5完整结果和中间失败分别保存在docs/motion-v5-competition-comparison.json及
  v5修正说明引用的历史证据；不沿用v4/v3时间和哈希。默认PP、LQR实验，比赛门限未放宽。
  当前/选中/下一周期停车余量分别为current_stopping_margin、selected_stopping_margin、
  selected_next_stopping_margin；constraint指出边界或本次障碍索引。上述历史余量字段用于诊断，
  v4提前余量优先排序已撤销；v7另外加入采用约束及多拍后备检查，不能混为恢复旧的排序。

  summary.statistics.phases列出各阶段duration_ms/distance_m、unexpected_stop_ticks、
  unexpected_stop_episodes、unexpected_stop_command_ms、navigation_reason_ticks和routes。
  routes.generations包含首次和新目标，不自动等于重规划次数；recent_generations最多16项。
  最后制动单列statistics.final_braking，与阶段数据之和对应总elapsed_ms/distance_m。
  Stop命令时长不等于实车静止时长；这些统计不需要--trace、不逐tick刷写eMMC。

  Worker是Rust库接口，没有新增“启动电机”CLI：
  同步AutonomyController::tick在Safety后采用命令；spawn后台tick_with_projection分开
  源测量和模型规划状态。只有输出线程poll的ControlPoll.command会推进已采用命令历史。
  latest.command仅诊断，不能绕过poll的故障/超时结果。
  try_submit(input, now)中的now是当前同一单调时钟，input内captured_at保持真实源时间。
  源比最近poll旧时可查询128条命令变化历史，不再一律ExecutionAhead；超出历史则
  HistoryUnavailable，不能改写捕获时戳。Busy可重试，Duplicate不重算/不续租，Stopped为停止。
  poll还检查采用窗口、真实命令变化前提和原始源期限；过晚/前提变化的Drive不直接采用。
  证书用有向矩形覆盖历史最高速度/曲率、原期限后额外一拍和完整刹停；静态停车空间、
  正常速度/转向/侧向约束均保留。0.9m合成直道回归通过，转弯狭小空间仍可能保守拒绝。
  共同时间轴、扫描关联/去畸变、实际反馈和物理执行接口仍待接车；详见
  docs/任务交接与异步运动修正.md。

终端求解与主机计算时间（v5）
[Mac；用途] 重放原短连接退化、检查共享求解额度，并测量完整模拟在本机的墙钟开销。

  cargo test --locked --offline -p xt-stcar-robot-core --lib navigation::terminal
  cargo run --release --locked --offline -p xt-stcar-robot-runner --example motion_profile -- 3
  cargo test --release --locked --offline -p xt-stcar-robot-runner --lib certificate_host_cost_measurement -- --ignored --nocapture

[替换项] 3为每种跟踪器测量次数，可填1至20；另有1次预热。需release构建。
[预期] motion_profile JSON报告每次wall_time_ms、completed/error及总/平均/最大墙钟。
  完整simulate耗时含合成RGB/激光、控制、车辆积分和sink遥测，报告序列化不计入；
  不是求解器独占时间、每tick延迟或RISC-V板卡WCET。测量时避免并行编译/重计算。
  最后一条显式运行默认忽略的私有证书基准，分别360/2048点，记录总/平均/最大耗时。
  常规测试忽略这项是为避免把主机调度噪声当成严格实时通过门限。

  navigation.diagnostics.terminal_work记录solver_attempts、iterations、primitive_samples，
  共享上限为256/1024/65536；primitive_samples是primitive前预扣额度，碰撞早退可少执行。
  solver_iteration_exhaustions表示单次8迭代用尽，budget_exhausted表示整拍共享额度不足，
  两者都不证明物理不可达。continued_seed_attempts/accepted记录重验接续初值的结果。
  summary.statistics.phases[].terminal_work统计总数和每拍最大值；日志额度不改变控制选择。
  v5历史证据见docs/motion-v5-validation.json、motion-v5-host-profile.json和
  docs/终端连接延续与异步停车包络.md。v6已补完整异步合成验证；实车制动/转向和目标耗时待车到后验证。

5.1 配置类型与入口
  config/competition-sim.json：SimulationConfig，含场景、理想车辆模型、
    autonomy、road、telemetry，供 autonomy-sim。
  config/competition-controller-sim.json：AutonomyConfig，只含控制器配置，
    供 autonomy-replay，不能拿完整 competition-sim.json 代替。
  config/road-perception-sim.json：RoadConfig，供 road-detect。
  config/scan-assembly-sim.json：Rust N10 全圈组帧库的配置参考。
  config/laser-localization-sim.json：Rust 激光定位库的配置参考。
  后两项目前没有独立 CLI 子命令，不编造 scan-assemble/localize 命令。
  示例 simulation_only/unverified 均不是实车标定。

5.2 查看并另存程序内置仿真配置
[Mac；用途] 获取与本程序版本一致的完整 SimulationConfig。
[替换项] 输出名选择新文件，不覆盖已人工标定或修改的配置。

  target/debug/xt-stcar-robot autonomy-example
  target/debug/xt-stcar-robot autonomy-example > work/commands/competition-example.json

[预期] 完整 JSON。后面的重定向是 shell 写文件，会覆盖同名文件，
  所以只写新的 work 诊断文件；不重定向到 config/ 或任何输入文件。

5.3 运行闭环仿真
[Mac；用途] 合成 RGB、光线投射雷达和理想位姿反馈，自动生成任务与导航命令。
[替换项] 无。离线模拟时钟不按真实墙钟等待。

  target/debug/xt-stcar-robot autonomy-sim --config config/competition-sim.json
  target/debug/xt-stcar-robot autonomy-sim --config config/competition-sim.json \
    --output work/commands/competition-run.jsonl

[预期] 无 --output 时默认 stdout 只显示 summary；指定文件时默认保留
  阶段变化、故障、terminal 和 summary。正常样例应观察到斑马线停足 3 秒、
  绕锥桶、灯区停车/绿灯确认、终点位置及航向满足后完成。
  实际结果以 completed、fault、final_actual_speed_mps、braking_ticks 等字段为准。
  故障退出码非零，并保存 terminal Stop；Stop 命令与制动后的模拟速度分开记录。
  理想位姿不代表真实激光定位精度，合成路面不代表实拍识别性能。

5.4 显式调试 trace
[Mac；用途] 定位任务/导航问题，按内存预算保留更详细的步进记录。
[替换项] 输出名可换；不支持虚构的 --trace-bytes CLI 参数。

  target/debug/xt-stcar-robot autonomy-sim --config config/competition-sim.json \
    --trace --output work/commands/competition-trace.jsonl

[预期] 详细记录加末尾结果。SimulationConfig.telemetry.max_trace_bytes
  默认 1048576（1 MiB），允许 1..8388608（8 MiB），仅约束可选 trace。
  超限增加 dropped_trace_records，不停止控制、不降低感知或控制频率。
  阶段/故障另有有限保留空间；异常计 logging_errors，不改变控制命令。
  terminal/summary 在结束时另外追加；离线 runner 集中写出，无每 tick fsync。
  进程异常退出前的纯内存日志可能尚未写出。必要实验数据按实际需要保留。
  eMMC 以正常运行和比赛为先：不为节写降低采样，不改 mount，不清理系统盘。

5.5 单张道路图片识别
[Mac；用途] Rust 斑马线几何、颜色灯和锥桶候选诊断。
[替换项] IMAGE 必须是本地 PNG/JPEG。地面矩阵与 ROI 示例不是本车相机标定。

  target/debug/xt-stcar-robot road-detect --config config/road-perception-sim.json \
    --image "$IMAGE" --output work/commands/road-cues.json

[预期] 单图 road_image_diagnostic JSON；无模型时使用显式灯 ROI。
  识别不确定应为 unknown，不把未检测到红色直接解释为绿灯。
  此命令不采集实时相机、不执行导航、不输出电机控制。

5.6 单图道路识别结合真实 YOLO 灯候选
[Mac；用途] Rust 原生 ORT 产生 class 9 灯候选，再由道路模块识别颜色/地面线索。
[替换项] IMAGE；显式传入模型和对应 Mac 动态库，两者必须同时给出。

  target/debug/xt-stcar-robot road-detect --config config/road-perception-sim.json \
    --image "$IMAGE" --model models/yolo26n.onnx \
    --runtime-lib toolchains/onnxruntime-macos-arm64-1.29.0/lib/libonnxruntime.1.29.0.dylib \
    --vision-config config/yolo26n.json --output work/commands/road-yolo.json

[预期] native_yolo_enabled=true 的单图诊断；模型 provenance 校验通过。
  YOLO26n 当前 80 类中的 9 是 traffic light，22 是动物 zebra；没有将
  斑马线、锥桶或红绿状态伪装成既有模型类别。道路规则/颜色识别独立实现。
  road-detect 不接受 --trace 或 --events。

5.7 强类型传感器快照回放
[Mac；用途] 将已经同步的测量快照交给同一自主控制器，由任务和导航生成命令。
[替换项] SNAPSHOTS 为已准备的严格 JSONL，不使用 robot-sim.jsonl 或串口原始日志。

  SNAPSHOTS='/替换为实际路径/sensor-snapshots.jsonl'
  target/debug/xt-stcar-robot autonomy-replay \
    --config config/competition-controller-sim.json --events "$SNAPSHOTS" \
    --output work/commands/snapshot-run.jsonl
  target/debug/xt-stcar-robot autonomy-replay \
    --config config/competition-controller-sim.json --events "$SNAPSHOTS" \
    --trace --output work/commands/snapshot-trace.jsonl

[预期] snapshot_summary，含 processed_snapshots、completed、fault 和日志计数。
  每行顶层必须为 at、pose、scan、road；不接受手工 Motion 字段。
  pose 是 PoseEstimate（位姿、实测速度/角速度、质量、frame、capture 时间）；
  scan 是 LidarSample（完整配置束数、范围、角度、capture 时间、雷达 frame）；
  road 是 RoadFrame（observation、图像宽高，以及可选elements）；observation 使用车体坐标。
  在线控制器要求elements存在且与pose/scan/observation同刻，元素数量与发布容量最多16；
  旧控制器可以没有elements。元素需真实几何、来源、误差和frame，不用人工目标坐标代替。
  at 使用同一启动纪元毫秒且严格递增，测量时间、年龄、frame 与配置一致。
  当前激光障碍投影要求 scan.captured_at 与 pose.captured_at 完全相同；
  斑马线几何锁定和视觉锥桶投影也要求 road.observation.captured_at 与位姿同刻。
  不同 capture 时刻须先正确关联/插值位姿，不能只改时间戳伪造同步。
  文件≤64 MiB、单行≤1 MiB、最多10000条；雷达≤1440束、视觉锥桶≤256个。
  过期、乱序、质量不可信或任务未完成就结束，会有 Fault/终端 Stop 和非零退出。
  默认 stdout 只显示 summary；--output 保存必要记录；--trace 额外保留有界调试。
  两次 serial-capture 有不同纪元，不能简单拼接成同步快照。
  Rust 组帧/定位是库能力；目前本 CLI 不把原始串口流自动变成真实闭环。
  JSONL 是开发诊断格式，不声称最终比赛允许录制/回放；初稿禁用 ROS1/bag，
  其范围仍需按最终规则确认。本工程命令不引入 ROS1、ros2 bag 或人工遥控。

六、Mac：打包、核验与未来上传

6.1 创建部署包
[Mac；用途] 以白名单打包当前已验证的两个 RISC-V 程序和运行配置。
[替换项] 无。先完成第 2.3 节构建；源码变化后必须重新构建。

  scripts/package.sh --help
  scripts/package.sh
  scripts/package.sh --model models/yolo26n.onnx --python .venv-model/bin/python

[预期] dist/ 下分别出现 core 和 with-model 包，并带同名 .sha256；
  文件名含版本时间/哈希，以脚本实际输出为准。包已存在时不覆盖。
  core 不含模型；模型仅在明确 --model 的 with-model 交付包里携带。
  两种包均不包含 .venv、.venv-model、工具链、Mac dylib、缓存、完整厂商资料。
  厂商 RISC-V ONNX Runtime/EP 库当前仍需单独核实，不由这个包自动提供。

6.2 检查待上传包
[Mac；用途] 核验压缩包/manifest/载荷哈希与内容，尚不连接车端。
[替换项] ARCHIVE 改成 dist/ 中本次实际包路径。

  ARCHIVE='dist/替换为实际包名.tar.gz'
  python3 scripts/delivery.py verify "$ARCHIVE"
  shasum -a 256 "$ARCHIVE"
  tar -tzf "$ARCHIVE"

[预期] verify 成功、包 SHA 与 .sha256 相同；内容是 bin/config/examples/scripts/docs
  及 manifest、SHA256SUMS、构建证据等。不要手工把环境目录加入包。

6.3 未来连接参数与上传预演
[Mac；用途] 只核验包和参数、显示传输计划。当前仍不执行车辆连接。
[替换项] 以下四项都按新车填写；release 是此前不存在的新目录名。

  VEHICLE_IP='192.0.2.10'
  VEHICLE_USER='vehicle'
  VEHICLE_PORT='22'
  VEHICLE_RELEASE="/home/${VEHICLE_USER}/xt-stcar/releases/first-check-REPLACE"
  scripts/upload.sh --help
  scripts/upload.sh --archive "$ARCHIVE" --host "$VEHICLE_IP" \
    --user "$VEHICLE_USER" --port "$VEHICLE_PORT" --remote-dir "$VEHICLE_RELEASE"

[预期] dry_run=true、extract_or_run=false，不执行 SSH/SCP。
  --host 必须为显式 IP，不能用 DNS 名或 SSH 别名；--user 必须为非 root。
  路径固定为 /home/账号/xt-stcar/releases/新名称；新名称只用字母、数字、_、-，
  开头为字母或数字。脚本拒绝覆盖已存在 release。

6.4 新车准备好后的首次 SSH 与实际传输
[Mac；用途] 未来明确接车后登录核对身份，再将已核验包传到独立目录。
[替换项] 先替换第 6.3 节参数，向现场或厂商核实 SSH 主机密钥指纹。

  ssh -p "$VEHICLE_PORT" "${VEHICLE_USER}@${VEHICLE_IP}"

[预期] 登录新车终端。首次提示的主机密钥必须与已核实指纹一致。
  在车端执行后面的只读检查，使用 exit 返回 Mac；不要在车端运行上传脚本。
  上传脚本需要已核实 known_hosts、已准备的非交互认证及车端 python3。
  它使用 BatchMode=yes、StrictHostKeyChecking=yes，不会提示输入 SSH 密码。

[Mac；用途] 明确传输，参数与 dry-run 保持一致，仅增加 --execute。
[替换项] 无额外替换；本条以后接车时才执行。

  scripts/upload.sh --archive "$ARCHIVE" --host "$VEHICLE_IP" \
    --user "$VEHICLE_USER" --port "$VEHICLE_PORT" --remote-dir "$VEHICLE_RELEASE" --execute

[预期] 新建独立 release、上传 bundle.tar.gz、远端 SHA256 核对成功。
  不解压、不安装、不运行程序、不修改服务。失败可能留下不完整新目录；
  检查原因后改用新 release 名重试，不通过删除或覆盖旧目录来掩盖失败。

七、车端：系统、存储、设备与出厂环境只读检查

以下各项只在以后登录新车后执行。工具不存在时记录“不可用”，不据此盲目安装。
本节未使用 sudo、服务启停、刷写、分区、格式化或系统升级命令。

7.1 平台和内存
[车端；用途] 核实实际系统、ISA、GLIBC、内存与网络地址。
[替换项] 无。

  uname -a
  uname -m
  cat /etc/os-release
  lscpu
  getconf GNU_LIBC_VERSION
  command -v ldd readelf file python3
  python3 --version
  free -h
  ip -br addr

[预期] 目标架构应为 riscv64；GLIBC/系统/内存以现场输出为准。
  Ubuntu/Bianbu 版本标签不等于所有动态库版本。这里不会把系统升级到 2.38。

7.2 磁盘、eMMC 和空间
[车端；用途] 确认根文件系统来自哪个设备，识别真实整盘及占用，不猜 mmcblk0。
[替换项] RELEASE 改为此次实际部署目录。

  lsblk -o NAME,PATH,TYPE,SIZE,FSTYPE,MOUNTPOINTS,MODEL
  lsblk -d -o NAME,PATH,SIZE,MODEL,TRAN
  findmnt -no SOURCE,FSTYPE,OPTIONS /
  df -hT
  RELEASE='/home/替换为账号/xt-stcar/releases/替换为本次名称'
  du -sh "$RELEASE"

[预期] 看到整盘、分区、挂载点和剩余空间。MODEL/TRAN 为空并不等于故障。
  根分区可能来自 SD/eMMC/其他盘，必须结合输出核对。
  若某 lsblk 列在当前系统不存在，先 lsblk --help，再使用已有列。

[车端；用途] 在识别整盘后读取其已有 sysfs 身份字段。
[替换项] BLOCK_NAME 填上一步确认的整盘 NAME，不填 /dev/ 前缀，不填分区号。

  BLOCK_NAME='替换为已核实整盘名'
  ls -ld "/sys/class/block/$BLOCK_NAME"
  ls "/sys/class/block/$BLOCK_NAME/device"
  for field in type name manfid oemid serial date; do
    attribute="/sys/class/block/$BLOCK_NAME/device/$field"
    if [ -r "$attribute" ]; then
      printf '%s: ' "$field"
      cat "$attribute"
    fi
  done

[预期] 只显示内核实际提供且可读的字段。不凭设备编号或单个字段断言 eMMC 5.1
  或剩余寿命百分比；版本和寿命信息需对应实物型号/厂商资料。
  本手册不读取视频、不执行写盘压力测试，不调整挂载策略或清理磁盘。

7.3 USB 和稳定设备路径
[车端；用途] 核实相机、雷达和 IMU 的设备身份，不打开数据流。
[替换项] SENSOR_TTY 填实际 by-id/by-path 链接；不要沿用示例 /dev/laser 的身份结论。

  lsusb
  ls -l /dev/serial/by-id/ /dev/serial/by-path/
  ls -l /dev/v4l/by-id/ /dev/v4l/by-path/
  SENSOR_TTY='/dev/serial/by-id/替换为实际传感器链接'
  readlink -f "$SENSOR_TTY"
  udevadm info --query=property --name="$SENSOR_TTY"
  fuser -v "$SENSOR_TTY"

[预期] USB 厂商/产品、序列号或物理路径、最终 tty 节点及占用进程。
  某目录不存在说明该绑定方式未提供，不直接猜 ttyUSB0 或 video0。
  fuser 无输出只表示当前权限可见范围内未找到占用，不保证所有驱动已停。
  不使用 fuser -k；也不打开底盘 /dev/car 试发数据。
  readlink -f、udevadm、lsblk、fuser 是这里的 Linux 用法，不照搬到 Mac。

7.4 出厂服务和 ROS 版本
[车端；用途] 只读服务状态/配置和已有 ROS 安装，不启动节点、发布消息或录 bag。
[替换项] VENDOR_SERVICE 根据第一条输出填写真实服务名，不猜固定名字。

  systemctl --type=service --state=running --no-pager
  systemctl list-unit-files --type=service --no-pager
  VENDOR_SERVICE='替换为实际厂商服务.service'
  systemctl status "$VENDOR_SERVICE" --no-pager
  systemctl cat "$VENDOR_SERVICE"
  printenv ROS_DISTRO ROS_VERSION ROS_DOMAIN_ID
  ls -ld /opt/ros /opt/bros
  ls /opt/ros /opt/bros
  command -v ros2 colcon
  dpkg-query -W 'ros-*-ros-base'

[预期] 记录启动命令、占用设备、安装目录和可见发行版信息。
  环境变量未设置不代表未安装；dpkg-query 不适用于非 Debian 包管理系统。
  不根据 Ubuntu 24.04 自动改用 Jazzy，不 source 未审阅的厂商启动脚本，
  不执行 launch、topic pub、service start/stop 或 bag 命令。

八、车端：独立解包与 ELF/运行库核验

8.1 在新 release 中核验并解包
[车端；用途] 手工检查已上传的本工程归档，保持厂商工作目录独立。
[替换项] RELEASE 填上传脚本输出的新目录；EXPECTED_SHA 填 Mac 核实的包 SHA256。

  RELEASE='/home/替换为账号/xt-stcar/releases/替换为本次名称'
  cd "$RELEASE"
  pwd
  EXPECTED_SHA='替换为Mac记录的64位十六进制SHA256'
  printf '%s  bundle.tar.gz\n' "$EXPECTED_SHA" | sha256sum -c -
  tar -tzf bundle.tar.gz

[预期] bundle.tar.gz: OK；归档成员是预期的相对路径。
  确认目录仅有此次上传包、归档已由本工程 verify 验证后，再解压：

  tar -xzf bundle.tar.gz --keep-old-files
  sha256sum -c SHA256SUMS

[预期] 每个载荷 OK。--keep-old-files 是 Linux GNU tar 选项，遇已存在载荷即失败，
  避免覆盖之前展开的版本；不要在出厂目录执行，也不要忽略校验失败。

8.2 程序静态兼容性
[车端；用途] 核对可信自建包的架构、加载器、依赖、GLIBC 版本需求。
[替换项] 无；readelf/ldd 存在时再运行。

  file bin/xt-stcar bin/xt-stcar-robot
  ls -l /lib/ld-linux-riscv64-lp64d.so.1
  getconf GNU_LIBC_VERSION
  readelf -h bin/xt-stcar-robot
  readelf -l bin/xt-stcar-robot
  readelf -d bin/xt-stcar-robot
  readelf --version-info bin/xt-stcar-robot
  ldd bin/xt-stcar
  ldd bin/xt-stcar-robot

[预期] RISC-V 64 位/LP64D，加载器存在，依赖没有 not found。
  ldd 仅用于本工程已核验的可信程序/库；未知下载文件先静态审查。
  ELF/ldd 成功不等于推理或停车已验收。报 GLIBC_2.xx not found 时记录实际输出，
  不替换 /lib/libc.so.6、不复制 Mac dylib，也不把其他 CPU 的库软链接冒充目标库。

8.3 可选 RISC-V ONNX Runtime C 库
[车端；用途] 推理前核实另外取得的可信 RISC-V 标准 ORT 动态库。
[替换项] ORT_LIB 和 ORT_DIR 改为已经核实的真实 .so 及其依赖目录。

  ORT_LIB='/替换为已核实RISC-V库路径/libonnxruntime.so'
  ORT_DIR='/替换为已核实RISC-V运行库目录'
  file "$ORT_LIB"
  readelf -h "$ORT_LIB"
  readelf -d "$ORT_LIB"
  readelf --version-info "$ORT_LIB"
  readelf --dyn-syms --wide "$ORT_LIB"
  ldd "$ORT_LIB"

[预期] 架构为 RISC-V、依赖齐全，动态符号表有 OrtGetApiBase。
  本工程要求标准 C API22；导出存在不能证明 GetApi(22) 和本模型实际可用。
  官方 SpacemiT 2.0.6 候选 ORT/EP 静态证据要求 GLIBC 2.38，
  EP 另要求 GLIBCXX 3.4.32 / CXXABI 1.3.15，目标执行仍需验证。
  LD_LIBRARY_PATH 只在下一节单次推理命令前设置，不全局 export，不替换系统 libc。

九、车端：可运行的离线程序与未来传感器读取

9.1 无硬件、无模型检查
[车端；用途] 在 ABI/载荷校验通过后验证 RISC-V 程序本身能运行。
[替换项] 无，工作目录保持本次 release。

  bash scripts/check-vehicle.sh
  bin/xt-stcar --help
  bin/xt-stcar self-check --config config/yolo26n.json
  bin/xt-stcar-robot --help
  bin/xt-stcar-robot replay --events examples/robot-sim.jsonl --config config/robot-sim.json
  bin/xt-stcar-robot autonomy-sim --config config/competition-sim.json

[预期] 只读环境记录、帮助、自检和离线仿真结果；不打开硬件。
  自检/仿真成功是目标程序运行证据，不能当作实际相机、雷达或车身停车验证。
  如需保存，使用独立目录并显式 --output，例如：

  mkdir -p run
  bin/xt-stcar-robot autonomy-sim --config config/competition-sim.json \
    --output run/competition-offline.jsonl

9.2 车端离线图片推理
[车端；用途] 在模型与目标 ORT 核验通过后，对磁盘图片验证实际推理。
[替换项] IMAGE 为车端图片路径；ORT_LIB/ORT_DIR 按第 8.3 节设置；需 with-model 包。

  IMAGE='/替换为车端实际路径/image.png'
  LD_LIBRARY_PATH="$ORT_DIR${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}" \
    bin/xt-stcar infer --image "$IMAGE" --model models/yolo26n.onnx \
    --runtime-lib "$ORT_LIB" --config config/yolo26n.json --output run/target-detections.json
  LD_LIBRARY_PATH="$ORT_DIR${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}" \
    bin/xt-stcar-robot road-detect --config config/road-perception-sim.json \
    --image "$IMAGE" --model models/yolo26n.onnx --runtime-lib "$ORT_LIB" \
    --vision-config config/yolo26n.json --output run/target-road.json

[预期] 实际目标推理 JSON，或明确动态库/API/模型错误。每条环境变量赋值仅作用于
  后面的一次命令。run 目录须先创建；图片不是实时相机源。

9.3 准备并预览本车 N10 采集配置
[车端；用途] 将已确认的传感器身份写入独立配置，再只看采集计划。
[替换项] 下面 device 替换为已核实的 N10 by-id/by-path 链接；选新的配置文件名。

cat > run/serial-n10-local.json <<'JSON'
{"device":"/dev/serial/by-id/REPLACE_WITH_VERIFIED_N10","protocol":"n10","duration_ms":1000,"read_timeout_ms":10,"max_bytes":65536}
JSON
  bin/xt-stcar-robot serial-capture --config run/serial-n10-local.json \
    --output run/n10-read.jsonl

[预期] 计划显示 N10、230400、明确设备和采集限额，不打开串口。
  WIT IMU 对应 protocol 为 imu_wit11，自动使用 115200；另外创建 IMU 配置，
  不把协议名和实际设备混配。本命令不自动停止占用端口的厂商服务。

9.4 以后实际只读传感器采集
[车端；用途] 在现场核实传感器身份、协议、端口占用和厂家服务后，明确读取 N10。
[替换项] 使用上项已核实的配置；输出名选择新文件。

  bin/xt-stcar-robot serial-capture --config run/serial-n10-local.json \
    --output run/n10-read.jsonl --execute

[预期] 读取达到时长/字节/记录限制后结束，JSONL 包含原始数据、Tick 和摘要。
  --execute 会配置/打开 tty，但不调用设备写命令；独占或权限失败会报错。
  默认参考上限不是实车测量参数。配置受时长≤60s、字节≤512KiB等资源边界约束。
  故障不覆盖已有采集日志；采集数据没有 Arm/Start/Motion，不能直接用于驱动车辆。
  串口读取成功不代表已完成全圈覆盖、定位、TF、物理标定或闭环控制验收。

十、Mac：源码 main 分支同步与 GitHub 上传

本节是用户约定的源码同步用法，与第六节车端部署分开。
远端应为 https://github.com/kxkxkxa767/XT-STCAR.git。
不上传模型、.venv、工具链、target、dist、缓存、镜像、大型厂商资料、密钥密码。
模型只在明确选择的 with-model 车辆部署包中携带，不进 GitHub 源码仓库。

10.1 每次修改前检查远端
[Mac；用途] 直接在 main 开发，先核对本地工作与云端更新。
[替换项] 无。

  cd /Users/yuhaojin/Documents/XT-STCAR
  git status --short
  git branch --show-current
  git remote -v
  git fetch origin
  git log --oneline HEAD..origin/main
  git rev-list --left-right --count HEAD...origin/main

[预期] 分支 main、远端正确；最后两个数字是本地独有、远端独有提交数。
  0 0 表示一致；仅远端有更新且本地改动已妥善保存时，先执行：

  git pull --ff-only origin main

  拉取后阅读对方修改再动文件。fetch/pull 失败不能视作“云端没有更新”。
  本地已有未提交内容先核对归属，不用 reset/clean/覆盖文件来获得干净状态。

10.2 检查、精确暂存、提交
[Mac；用途] 完成相关验证后，只提交本次实际改动。
[替换项] 路径和提交说明改为本次内容；不直接 git add . 包含无关文件。

  git diff
  git add -- '替换为本次文件路径'
  git diff --cached --check
  git diff --cached --stat
  git diff --cached
  git commit -m "替换为具体改动说明"

[预期] 提交只含本次源码、配置、文档、锁文件及必要验证记录。
  说明举例：“补充比赛状态机航向约束与回归测试”；不要只写 update。

10.3 推送前再次同步
[Mac；用途] 处理开发期间队友新提交，不新建功能分支/PR，不强推。
[替换项] 无。

  git fetch origin
  git rev-list --left-right --count HEAD...origin/main

  若远端独有为 0：
  git push origin main

  若两边都有新提交，仅将本地尚未推送的提交重放：
  git rebase origin/main

  有冲突先核对双方内容，编辑后：
  git add -- '替换为已解决冲突的文件路径'
  git rebase --continue

  完成后重新验证受影响内容，再执行：
  git push origin main

[预期] 正常快进推送；禁止 --force 和 --force-with-lease。
  不重写已发布提交，不通过单方面保留某一版本覆盖队友成果。

10.4 核对上传结果
[Mac；用途] 确认本地和 GitHub 的 main 提交一致。
[替换项] 无。

  git status --short
  git rev-parse HEAD
  git ls-remote --heads origin main

[预期] 本次文件已提交；本地与远端提交哈希一致。忽略目录中的本机构建产物仍留本地。

十一、赛前测量后生成固定布局（Mac，保留的历史模式）

  原7×5米简化场景的8场矩阵是时序回归，并非8种尺寸。
  新模块支持赛前填入米制实测规格，启动前编译并冻结；它不会自动巡视测场。
  规则初稿：外围长5–8米、宽4–6米，上下直道分别1–2米；还须满足区域/锥桶/车体组合校验。
  不缩放0.8米发车终点区、纸条、0.28米锥桶底座、车辆或控制限值。

[在Mac执行] 创建输入模板（不要覆盖已有现场测量文件）：
  cd /Users/yuhaojin/Documents/XT-STCAR
  mkdir -p work/field
  cargo run --locked --offline -p xt-stcar-robot-runner --bin xt-stcar-robot -- field-example > work/field/spec.json

[怎么改] 编辑spec.json。字段解释见docs/赛场规格自适应.md。
  spec内填外场长宽、上下直道宽度/跨度、左右锥桶距对应侧墙、左桶距上墙、右桶距下墙、
  斑马线搜索区起点、灯前禁越线x和停车区长度；坐标原点在左下，+x向右、+y向上，全部米制。
  source=measured只标注测量来源，不启用硬件或代替底盘/相机/定位标定。
  外层crosswalk_near_x_m是合成图像真值，Mission不直接用它当停车点。
  cone_route_radius_m是工程首选半径；实际半径会按侧墙和灯前区域空间约束生成。

[查看布局] 输出JSON包含各区域、锥桶、实际选择半径、带朝向通过点、灯前和终点坐标：
  cargo run --locked --offline -p xt-stcar-robot-runner --bin xt-stcar-robot -- field-layout --config work/field/spec.json --output work/field/layout.json

[编译并跑闭环] layout.json只是布局；autonomy-sim使用下面生成的compiled.json：
  cargo run --locked --offline -p xt-stcar-robot-runner --bin xt-stcar-robot -- field-compile --config work/field/spec.json --output work/field/compiled.json
  cargo run --release --locked --offline -p xt-stcar-robot-runner --bin xt-stcar-robot -- autonomy-sim --config work/field/compiled.json --output work/field/run.jsonl

[预期] 非法规格拒绝；编译失败保留旧输出。不能单独篡改compiled里的任务坐标。
  JSON重新读取允许的只是浮点末位舍入误差，厘米级修改仍拒绝。
  修改实测规格后须重新编译并重新创建控制器；运行中不会悄悄移动目标。
  模拟完成看completed/fault、实际停稳与绿灯确认、全车终点和结束状态，不只看进程生成了文件。
  该模式不会自动测全场或运行中重新认桶；新在线模式另见下一节，真实灯前/终点标记仍未确认。

[完整场地矩阵] 固定9种布局×同步/异步×PP/LQR，失败记录不会被删除：
  cargo run --release --locked --offline -p xt-stcar-robot-runner --example field_matrix -- both all both > work/field/matrix.json

[单场诊断] 可分别选择sync/async/both、场景名/all、pp/lqr/both：
  cargo run --release --locked --offline -p xt-stcar-robot-runner --example field_matrix -- sync small_5x4 pp
  cargo run --release --locked --offline -p xt-stcar-robot-runner --example field_matrix -- --help

  任何未完成、故障或验收未满足均非零退出，仍输出JSON供定位。
  九场覆盖有限测量组合，不保证官方连续区间内所有组合都能成功；PP默认、LQR实验。
  异步功能钟等待worker会冻结，这不是车端实时性或WCET验证。
  默认不增加每tick落盘；可选诊断保持有界，比赛控制不为eMMC寿命降频。

十二、在线元素驱动任务（Mac，离线模拟）

  任务顺序固定，锥桶、斑马线、停车区和终点几何由传感器观测更新。
  FieldSpec只供模拟器摆物体、生成RGB/雷达与裁判真值；不同场地使用逐值相同的在线控制器配置。
  截至2026-09-16，冻结源码后的最终矩阵4/19符合预定检查，正常场完成0/15，在线比赛闭环尚未验收。
  通过项仅缺标记、缺锥桶各同步/异步安全停止；14场裁判证明两桶后仍在ApproachLight因有灯色而缺确认停车区几何停止。
  三个小场均未通过第一桶，同步46.200秒、异步43.863秒、扰动异步45.045秒结束；不能把所有失败都归为灯区缺信息。
  最新legacy-integration旧八场8/8完成、原性能门通过；九项关键指标、序列化输出计数和八项单因素比较与v9精确一致，
  未声称逐命令或完整轨迹逐点相等，也不等于在线比赛验收。
  最终Rust工作区565通过、0失败、3项按设计忽略；实际日志另记录tracking/timing/online example为2/5/2通过，Python交付37通过。
  fmt、all-targets clippy -D warnings、双RISC-V链接及ELF检查通过，核对150份编译输入；例程/Python日志不另推断源码同一性。
  目标基线GLIBC2.38、实际最高引用2.34；xt-stcar为1,217,056字节/libc，robot为2,376,448字节/libm+libc。
  矩阵运行前后源码及实际命令/可执行文件/原始输出哈希已核对；本轮没有目标执行或接车。
  两包路径、哈希与核验见包外docs/online-mission-delivery.json；Git提交和远端同步状态以实查为准。
  旧固定布局35/36不作为在线验收，实际成功和失败见online-mission-*正式报告。
  原PP、运动限制、完整车体、制动和异步采用检查保留；未知空间不作为空地，仍无电机输出。

12.1 创建场景、编译并运行
[Mac；用途] 生成在线场景，连续使用合成RGB/雷达/位姿反馈；不需要模型或ORT。
[替换项] 选择新的场景/输出文件名，避免第一条重定向覆盖已有测量或实验记录。

  cd /Users/yuhaojin/Documents/XT-STCAR
  mkdir -p work/online
  cargo run --locked --offline -p xt-stcar-robot-runner --bin xt-stcar-robot -- online-example > work/online/scenario.json
  cargo run --locked --offline -p xt-stcar-robot-runner --bin xt-stcar-robot -- online-compile --config work/online/scenario.json --output work/online/compiled.json
  cargo run --release --locked --offline -p xt-stcar-robot-runner --bin xt-stcar-robot -- autonomy-sim --config work/online/compiled.json --output work/online/run.jsonl

[怎么改] scene是模拟器的场地/元素真值；改它来验证元素移动，而非修改在线任务必经坐标。
  experimental_markers_enabled显式选择实验标记。occlusions声明有限时间的视觉遮挡：
  from_ms、through_ms是模拟毫秒区间，cones/markers选择遮挡对象；雷达中的真实物体仍保留。
  online-compile先完成校验才提交输出；非法输入保留旧输出，不把成功编译当作成功完成比赛。

[预期] summary记录completed、fault和终态；非预期失败保留日志并非零退出。
  默认只记阶段、故障和终态，--trace才增有界调试；模拟秒数不是主板计算耗时。
  交付文件约定：模板config/online-example.json，已编译场景config/online-sim.json。
  config/online-controller-sim.json是纯控制器配置，只用于带road.elements的同步快照回放，
  不直接把它传给autonomy-sim，也不把online-sim.json当作纯控制器配置。

12.2 场景矩阵与失败检查
[Mac；用途] 查看预先声明的场地/元素移动、遮挡、缺失和时序扰动场景，再运行指定项。
[替换项] nominal可替换为cases输出的实际场景名；不是任意文件名。

  cargo run --release --locked --offline -p xt-stcar-robot-runner --example online_matrix -- cases
  cargo run --release --locked --offline -p xt-stcar-robot-runner --example online_matrix -- sync nominal
  cargo run --release --locked --offline -p xt-stcar-robot-runner --example online_matrix -- async-trace nominal > work/online/nominal-async-trace.json
  cargo run --release --locked --offline -p xt-stcar-robot-runner --example online_matrix -- both all > work/online/matrix.json

[预期] 模式为cases/sync/async/async-trace/both，选择场景名或all；时序扰动仅跑异步。
  看逐项expected_completion、passed、summary.completed和fault，不能只数JSON里的结果条数。
  缺标记或缺桶场景应安全未完成，需核对故障与最终停止，不能把任意失败都算安全通过。
  正式报告是docs/online-mission-validation.json、online-mission-matrix.json和online-mission-experiments.json。
  保留82.8秒同步开发回放：已进入两桶后的灯前阶段，缺区域几何而Stop/故障，未完成整场。
  保留84.383秒异步开发回放：第二桶接近形成长回环，20秒视觉语义到期后结束有限重观察并故障。
  新入口切向Stop与浮点稳定化已实现；更早47.1秒桶搜索失败仍保留。
  首轮2/19项符合检查，均为缺桶安全停止。移动临时目标附航向的stable-entry-small失败也保留；
  后续改为稳定方向目标的两段horizon；通用点连接初版造成的旧八场性能退化也保留在legacy-point-timing-final。
  第二轮3/19、第三至第五轮各4/19是历史阶段数据，各轮失败分别保留。
  最终矩阵仍4/19且正常完成0/15，14场真实通过两桶；原始结果及源码运行凭证已归档正式报告。
  最新legacy-integration旧八场已恢复，不能代替在线结果。
  不删除失败或拿历史矩阵替代，不宣称在线比赛闭环已验收。

  summary.online_referee只读真实场景锥桶和车辆实际积分轨迹，独立核对左右侧、顺序、半圈进度、
  入出航向与车体碰撞。查看order_valid/trajectory_valid/both_completed/failure_reason，
  控制器processed_cones不能充当实际通过证明；还须检查停车、绿灯、终点、实际末速度/曲率和fault。
  裁判真值不进入控制器，以上仍是合成模拟证据。

  同步recent_online_attempts最多末16项（通常至少100ms间隔，终态可提前记录）；
  异步recent_attempts最多末16次发布尝试，包含被拒绝/故障的报告。active_track可能是失效前最后几何，
  不能仅凭字段存在判断目标有效。显式async-trace才额外输出offline_plan_trace，最多前2048个计划时刻，
  超出数量见omitted_trace_plans。这些有界诊断仅用于模拟，不加入控制快照、不代替ControlPoll.command。

12.3 真实图像接口和实验标记边界
  Rust库RoadPipeline::new_online(road, vision, markers)常驻处理RGB；vision=None不调用ORT。
  process按车体前方消除地面线180度方向双义；process_with_heading可显式给预期车体方向，
  该参数不是世界坐标或场景真值。旧new入口保持elements=None。
  视觉桶保留颜色并经同刻雷达唯一簇关联；last_visual_at记录真视觉、last_geometry_at记录最近有效几何，
  last_seen兼容后者。新雷达只维护已经确认的同一锥桶，须满足连续唯一簇、旧半径、同刻位姿及明确外参，
  不创建新桶、不猜颜色、不增加视觉确认数；矛盾/分裂/歧义/短弧不确定性过大不续期。
  track_ttl_ms=1800：无新有效几何满1.8秒失效；cone_semantic_ttl_ms=20000：无新视觉满20秒失效，
  即使每拍有雷达也不能延长该视觉期限。20秒参考0.85m半圈以0.18m/s约14.84秒并留有限余量，
  较慢或回环轨迹须重新观察；旧配置缺语义期限字段时重新编译场景，不能静默延长TTL。
  几何误差保留最近视觉下限，并独立计算本次拟合/量程/位姿及短弧不确定性，不逐拍重复累加，也不缩成激光0.02m。
  已处理StopLine/FinishMarker仍可由真实同ID观测刷新边界，processed始终为true、不能再选为新任务；
  Cone/Crosswalk保留冻结记录，矛盾身份不会自行恢复。
  StopLine/FinishMarker槽内真实唯一重观察可保留ID/first_seen/processed；跨过原TTL首新采集帧将
  observations重置1，默认第二不同采集时刻的真实帧才恢复确认；该计数表示当前确认周期的独立视觉帧数。
  invalid/歧义不能复活，未重见不刷新时间；锥桶确认规则不变。runner显式pin至多两个当前引用的区域槽，
  防止存储身份回收；pin不延长TTL、不恢复invalid、不跳过两帧确认，过期几何仍不能作为有效tracks。
  未保留且已回收的槽不能凭空找回原ID。
  浮点坐标往返仅在32EPS相对尺度内保留上一表示，最多1nm位置/1e-9rad方向；米级实际约1e-14。
  这不是测量滤波：毫米变化、误差、观测时间和真实确认数仍更新，避免纯roundoff清空导航缓存。
  局部凸包KnownFree保留原外接圆成功路径，再用固定栈按长边自适应二分外包矩形，总计最多64次查询。
  每个接受叶矩形须完整包含于已证空闲圆，子矩形共享同一分界；未知、无法细分或额度耗尽不能漏验放行。
  Obstacle独立用固定容量、最多32点凸包与同源真实有限回波的欧氏距离，加原量程/位姿/年龄误差判断；
  回波只落在覆盖圆或外包矩形多出的区域，不代表请求凸包碰撞。旧均匀覆盖只用于回归，生产已用自适应证明。
  绕桶候选用KnownFree/Obstacle/Unknown三值筛选，有距离证据的障碍优先拒绝；未来Unknown只是假设，
  当前目标车体和完整执行停车包络仍必须KnownFree，不能把候选被保留理解为准许Drive。
  前方短弧尚非完整KnownFree时提前采用原approach限速0.18m/s；完整空闲才允许cruise上限0.3m/s。
  降速不能代替当前执行空闲证明。斑马线停车目标另减完整region_error，包含位置误差及
  hypot(区域深度,区域半宽)×航向误差，不遗漏旋转导致的边角位移。
  切向矩形绕桶的R下界按桶半径+实际车体半宽+净空+位置误差，并检查原最大曲率；
  前后悬仍纳入分段车体凸包及角点圆弧弓高的全扫掠证明，未缩小车体。
  对齐时保持仍可行的已选R，必要时试安全下界内的较小R，不因视点变化无故向外跳；
  对齐/入弧后若原R不能满足更新后的安全下界则停止。
  绕桶完成还须实际沿出口方向越过桶中心位置误差；圆顶出口目标沿切线延伸“位置误差+2×原目标容差”，
  保留原外侧、半圈、位置与航向条件，进度仍封顶π，不把切线延伸虚算转角。原容差数值不变。
  出口前约5–6mm、恰在中线或仍在中心误差内，不能仅靠near目标就提前计为已处理。
  剩余转角为零时仍按实际绕行切向证明完整车体和净空KnownFree，不直接判Unknown或自动放行；
  原真实出口投影、半圈与到达条件全部保留。
  接近绕桶入口若尚未对齐，使用原ArrivalBehavior::Stop在局部切向引导点满足位置/方向/停稳条件，
  再进入短弧；已满足切向条件可直接接近，不加定时hold，不改原容差、限速、预算与已知空闲检查。
  带明确航向的稳定观测目标允许两个局部horizon（默认1.6m）供现有Nav侧向接近和切向对齐；
  超过该范围仍是单horizon（默认0.8m）、无终端航向的临时引导点，搜索也保持单horizon。
  不给不断移动的临时点硬附heading。该版本整场结果见最终报告，正常场仍未完成。
  Nav对真实26580ms worker状态的短连接漂移增加同目标到达区域重认证，仍在原共享预算记账，
  不重置求解/采样额度；目标/方向/到达方式/边界变化不能沿用。单快照回归不代表整场完成。
  Navigator初始化固定TargetPolicy：Fixed为默认，在线统一RollingLocal，不按场地结果开关。
  仅RollingLocal无终端航向目标在[0.35m,min(2×lookahead,2m))内先试同账前进单弧；严格中心连接失败，
  才可认证真实积分端点及累计误差进入原半位置容差，不吸附目标、不重置预算。Fixed保留原搜索顺序。
  该分支覆盖49580ms滚动点快照。21380ms定向斑马线接近则在原单/双弧后备失败后，对RollingLocal
  增加目标曲率为零的短前进候选：保留当前执行曲率，按原模型渐变积分，不直接拉直车头。
  长度取目标的车体前向正投影、目标距离<0.35m；真实端点及累计误差仍须满足原半位置/航向容差，
  完整车体、边界和采用条件不变；新尝试与旧失败共享原预算。最新legacy-integration旧八场已恢复，非在线整场证明。
  对<0.35m的定向RollingLocal目标，顺序为原严格双弧→原严格单弧→原到达区域→lattice，
  同一账本下对当前新boundary重证。实际18100/18800ms短路线变3.70/7.22m回环的问题据此修正；
  三个邻帧原严格连接成功保持，Fixed搜索顺序不变。
  原region及零目标曲率候选均失败后，RollingLocal可追加“保持实际已应用曲率→按原rate回中→直行”
  三段短接。航向公式仅给段长种子，三段经原primitive积分并连续携带曲率和误差；真实终点仍须进入
  原半位置/航向容差，总长及求解/迭代/采样同账有界。38400/38500真实帧得到短接，38300原成功和
  Fixed三帧保持；越线、障碍、累计误差、预算与非法输入负例仍拒绝，不能把定向回归当整场通过。
  动作连续性另在RollingLocal原strict continuation候选全失败后，从各候选完整周期预测姿态、实际曲率
  及累计误差调用旧oriented_arrival_region，同一账本证明后续可达；不调用上面的新三段候选，
  不直接判任务到达或adopt，原碰撞/制动/采用门保持。
  仅RollingLocal连续recovery且有限侧带边界，原胶囊边界失败后可补整车扫掠二证：两端完整车体凸包
  加L/2×(1+Kmax×车体外接半径)，再加原净空/重启余量、累计位置/航向误差和向外数值余量。
  整个包络须处于同一允许半空间，不以各角分别在禁区两侧放行；世界矩形与障碍仍原完整胶囊证明，
  Fixed、全局边界与只有位置的参考段检查不变。
  原胶囊及一阶界都失败后，可再用实际MotionTransition做二阶完整车体时间域证明：
  M=A+V²K+r(AK+VS+V²K²)，整个运动落在两端完整车体凸包加M×dt²/8内。V/K取initial/target上界，
  A取原加减速度较大者、S取原转向速率；饱和处v/κ及车体点一阶导数连续，恒速/恒曲率也不减A/S。
  primitive按同次恒规划速度ds/max_speed取dt；rollout按实际加减速缩尾求解，与predict共用duration.min(dt)，
  不从实测低速倒推时间。端点累计位置/朝向误差、净空/重启余量和浮点外向余量全部保留，
  world/obstacle仍原胶囊、禁带仍共同分离侧、Fixed提前退出；不增加采样或求解预算。
  online32000_body_chord_tube.json保存32000ms及相邻四个真实输入。原strict受粗界拒绝、region成功的
  邻帧可能改为strict优先成功，不能声称全轨迹不变；32100历史停舵状态找到短route仍不等于可执行Drive。
  加减速、饱和顺序、减速到零、曲率穿零、真实越线和误差/非法输入有专项回归，最终整场另据实际报告填写。
  仅RollingLocal在原停车disk证明失败时，可追加完整StoppingEnvelope证明：原两控制周期反应距离
  加全制动距离，保留当前/候选/历史速度上界，并用原Kmax覆盖期间任意正负转向变化和完整车体，
  不假定沿固定名义弧刹停。原rollout、误差、采用和最终来源检查继续保留，Fixed不走该后备证明。
  实际制动力不低于配置仍需车端标定，不能把模型证明写成实车制动验收。
  runner的OrbitSpeedHint只用于OrbitCone：实际pose加五个观测短弧参考姿态，以原Kmax和
  感知年龄加控制周期horizon检查完整停车包络KnownFree；原upper已有证明则保持，否则最多八次二分
  只取已证正下界，同步限制target与continuation上限。没有证明不伪造零速目标，也不改写真正较高速度。
  六个参考姿态只作选速提示，不证明连续运动安全；实际source/history/negative/lease与执行门全部保留。
  仅绕桶且停车线负约束尚未获绿灯释放时，Nav前还以当前原始source姿态的完整StoppingEnvelope
  同时检查KnownFree和pending/confirmed线，沿原观测/checked时钟增加误差；原上限能证则保持，
  否则最多八次二分取已证正速度上限，同步限制当前目标与continuation，不改线位置/时间/确认/TTL。
  提示还要满足当拍可执行下界：同源测量或规划投影速度，按原减速度和真实tick间隔计算（最多原period），
  再与有效adoption速度区间下界取较大值；source/planned时刻不匹配则不使用。
  几何cap低于可执行下界时不向上凑未证速度，也不假装已减速；提示缺席后仍由独立实际执行门决定Stop。
  permits_command/异步证书继续使用真正source、投影、已采用历史速度/曲率和全部负线，不拿假设低速替代。
  开发async33260的OpenEmpty发生在rollout之前，没有预算耗尽或进入permits；只能说明原约束下
  有界规划未找到路径，缺完整缓存时不能宣称几何无解或source gate直接拒绝。最终整场另据正式结果。
  当前没有把USB相机、传感器采集与电机发送串成现场自动驾驶CLI。

  官方文字未确认灯前/终点可见图案；当前单/双白横条是明确的实验协议。
  bar_span_m只表示视觉条纹跨度；region_width_m独立声明可用子区宽度。默认灯前0.6m条对应0.8m宽子区，终点0.9m条对应0.9m宽子区，不从场地真值读取；真实场不保证存在这些标记。
  ExperimentalMarkerConfig默认enabled=false，online-example仅在示例中显式启用。
  检测器从RGB几何恢复远沿附近图案锚点，按声明深度还原区域近沿；不从灯色推导距离。
  图案缺失/裁剪或区域信息过期时不能凭绿灯继续完成任务，仍须原停稳与绿灯确认。
  已取得官方资料没有左桶出口到灯的可用距离正下界，不能按示意图比例造距离。
  当前固定灯色ROI只能识别颜色，不能定位停车区；模拟中可比地面标记更早可见，形成信息缺口，
  此时不能凭灯色继续猜距离，可能Stop后超时。这是第一次区域几何尚未获得的信息缺口。
  第二个独立缺口是曾确认的地面标记在车尾清空前退出前向ROI：特定默认直行中，最后完整可见点
  约为远沿前0.26m，清尾至少到远沿后0.18m；剩余0.44m以0.18m/s约需2.44s，计既有误差约4.89s。
  这些只是在默认直行条件下的配置估算，不是全场不可行证明或实跑成绩；终点完整双条也可能裁剪。
  不把里程计预测伪造为fresh，不新增stale线的正向任务获取、绿灯或清尾授权。过期StopLine负约束
  继续按原来源时钟增加位置/方向误差；整个包络仍可在保守线前或扩大侧带之外通行，不在TTL到期时
  突然封死整条带。需要正向地标证据的任务仍受原有限重观察与停止条件约束。
  Completed显式要求全车清空灯区。合法确认的终点即使与灯区重叠，已到达停稳却未清尾仍Fault，
  不提前mark_processed；部分重叠但实际清尾已获证明可以继续，标记名称不替代空间顺序证据。
  后续可评估保持真实地标可见的局部轨迹、唯一锚点的部分条边跟踪；无此条件就需新定位测量或
  明确可验证的局部先验，不能只写等接车实测，也不能臆造现场标记。当前不能保证在线整场完成。
  真实现场标记、相机标定、共同采样时钟和雷达外参仍需核实，当前暂不接车。

十三、结果判断与资料入口

  命令成功后可单独执行 echo $? 查看上一条退出码；0 为成功，非零先看 stderr。
  别在另一条命令之后再读取 $? 并误认为还是原命令结果。
  离线任务故障应记录具体 fault 与 terminal Stop；看不到物理输出是当前设计边界。
  未知灯色、旧帧、定位失效、扫描缺失等应停车/锁存，不能靠重试绕过安全状态。
  模拟记录中的速度、距离、制动和完成仅对声明的合成场景有效。
  正式接车仍需真实传感器接入、共同采样时钟、TF、路面与底盘标定、运行库验收、
  MCU 反馈/watchdog 和现场停车验证；当前没有一条“启动自动驾驶电机”的命令。

  agent.md：完整交接和长期约定。
  README.md：代码结构、模块与当前入口。
  上传规范.md：main 修改前同步、提交与推送。
  资料/环境.md：Mac、Windows 与车端环境来源；早期验证数字以最新记录为准。
  docs/部署包使用说明.md：打包、上传、载荷与依赖边界。
  docs/Rust串口采集.md：传感器采集配置与限制。
  docs/Rust比赛自主闭环.md：任务、导航、感知 worker、仿真及日志设计边界。
  docs/任务交接与异步运动修正.md：v4真实验收半径、阶段统计、执行历史和异步采用证书。
  docs/采用时基与时序交叉验证.md：v8共享last-change时基、检查顺序及2×2矩阵入口。
  docs/motion-v9-before.json：历史v9分享/ZIP来源与同姿态旧剩余路线增大证据。
  docs/路径连续性与性能回归.md：v9数值初猜接续、固定性能门和验收边界。
  docs/motion-v9-route-regressions.json、motion-v9-experiments.json：7项专项覆盖与废弃方案。
  docs/motion-v9-validation/build/delivery.json：历史v9验收、编译输入/ELF和本地包；分别是三个文件。
  docs/motion-v8-before.json：历史v8分享/ZIP/探针来源与隔离旧版失败。
  docs/motion-v8-baseline-matrix.json：修改前完整8场及有界轨迹首次分叉。
  docs/采用约束与前向恢复.md：v7共享采用条件、结构化首因及前向恢复设计历史。
  docs/motion-v6-validation.json：历史v6的Rust/example/交付测试、fmt/clippy与交叉链接结果。
  docs/motion-v6-delivery.json：历史v6本地包状态/路径/哈希；不能作为当前版本产物。
  docs/终端连接延续与异步停车包络.md：v5接续策略、共享计算额度及有向停止矩形。
  docs/motion-v8-*、motion-v7-*、motion-v6-*、motion-v5-*、motion-v4-*及更早结果保留历史。
  docs/底盘标定映射.md：物理单位映射和未经验证的示例表。
  docs/赛项三规则与工程差距.md：初稿规则、资料证据与实车缺口。
  docs/赛场规格自适应.md：场地测量、半径/通过点生成、兼容处理与覆盖边界。
  docs/field-adaptation-matrix.json、field-adaptation-legacy-matrix.json：新场地与旧时序的独立记录。
  docs/field-adaptation-validation/build/delivery.json：历史固定布局验收、交叉编译与本地包，分别为三个文件。
  docs/在线元素驱动任务.md：任务拓扑、短期元素身份、局部空间、实验标记与在线入口。
  docs/online-mission-validation.json、online-mission-matrix.json、online-mission-experiments.json：本轮在线验收、矩阵和实际开发失败；未完成前不声明整场通过。

手册中的车端 SSH、设备访问和安装命令均未执行；源码 Git 上传状态以 agent.md 和本次交付记录为准。
