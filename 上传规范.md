# 上传规范

适用于本工程所有源码、配置、脚本和文档修改。仓库为
[`kxkxkxa767/XT-STCAR`](https://github.com/kxkxkxa767/XT-STCAR)，远端名称 `origin`，主分支 `main`。

## 基本约定

- 日常直接在 `main` 修改、提交并推送，不另建功能分支，不走 PR 或分支合并流程；用户另有明确要求时再调整。
- **每次开始修改前，先检查云端仓库有没有更新；有更新先拉取，再修改文件。**
- 修改结束后完成相应检查，使用 `git commit -m "..."` 简要说明本次具体改动，再推送 `main`。
- 保留已有本地改动和队友提交，不强制推送，不通过丢弃文件或重置分支覆盖他人工作。

## 1. 修改前同步

在工程根目录执行：

```bash
git status --short
git branch --show-current
git remote -v
git fetch origin
git log --oneline HEAD..origin/main
git rev-list --left-right --count HEAD...origin/main
```

确认当前分支为 `main`、远端地址正确。最后一条输出的两个数字分别表示本地独有、远端独有的提交数。
两项都为 `0` 表示已同步，可以开始修改；仅远端有新提交时，先执行：

```bash
git pull --ff-only origin main
```

拉取成功后阅读远端改动，尤其是 `agent.md`、本规范以及本次要修改的文件，再继续工作。
若本地、远端都有新提交，先按第 3 节整理本地尚未推送的提交；只有本地有提交时，先核对其归属和内容再继续。
如果当前不在 `main`，先保留原有工作，再切换到 `main`；不要把其他任务的改动直接带入本次提交。
如果存在未提交内容，先检查归属并妥善保存，不能直接覆盖或丢弃。
远端检查或拉取失败时，不视为“没有更新”；先解决同步问题，再开始依赖最新代码的修改。

## 2. 修改与提交

按改动范围完成验证：文档修改检查内容、路径和链接；Rust 代码修改执行相应测试、fmt/clippy，
需要交叉编译交付时再运行项目构建和打包脚本。涉及模块或使用方法变化时同步 README；交接状态变化时更新 `agent.md`。

检查差异，只暂存本次实际修改的文件：

```bash
git diff
git add -- 本次修改的文件路径
git diff --cached --check
git diff --cached --stat
git diff --cached
git commit -m "简要说明本次具体改动"
```

命令中的文件路径和提交说明须替换为实际内容。`-m` 用一句简短的话说明“改了哪个模块、做了什么”，
中文即可，例如 `新增 N10 雷达解析与回放样例`、`修复串口超时后继续写入的问题`、`补充 main 直接提交的上传规范`。
避免仅写 `update`、`修改` 或 `修复问题`；提交说明应与本次差异一致。

## 3. 推送及并发更新

提交后再次检查远端，避免修改期间队友已经上传新提交：

```bash
git fetch origin
git rev-list --left-right --count HEAD...origin/main
```

远端独有提交数为 `0` 时直接推送：

```bash
git push origin main
```

若本地、远端都有新提交，保持在 `main`，将**本地尚未推送的提交**重放到最新远端之上：

```bash
git rebase origin/main
```

出现冲突时核对双方改动、编辑冲突文件，`git add -- 冲突文件路径` 后执行 `git rebase --continue`；
不能简单用一方版本覆盖另一方。整理完成后重新检查受影响部分，再 `git push origin main`。
这一步不需要新建分支或 PR，也不改写已发布的远端提交。推送因远端再次更新而被拒绝时，重新 fetch、整理和验证，禁止 `--force` 或 `--force-with-lease`。

推送成功后核对 `git status --short`、`git rev-parse HEAD` 和 `git ls-remote --heads origin main`，
确认本次文件已提交、本地与云端 `main` 指向相同提交，再报告提交号和改动摘要。

## 上传范围

提交源码、配置、脚本、依赖锁文件、文档和必要的验证记录；遵守 `.gitignore`。
工具链、虚拟环境、缓存、模型大文件、系统镜像、构建产物、部署包及密钥密码不进 Git。
本规范描述 GitHub 源码同步，不改变车辆部署和硬件操作的授权范围；当前仍按“暂不接车”执行。
