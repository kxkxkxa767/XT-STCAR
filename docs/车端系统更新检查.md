# 车端系统更新检查

2026-09-16，用户要求“车上能更新的都更新”。已完成当前配置软件源的更新检查；本阶段无可执行的普通包升级事务，没有把检查结果写成全系统已经换新。

已安装并启用的项目更新是上一阶段的[ONNX Runtime用户级更新](车端ONNXRuntime升级.md)。系统Python、ROS、驱动及其他APT包均以当前Bianbu2.2厂商配套源和优先级为准，不跨发行版、不换源追逐上游版本。

## 实际检查

SSH账户没有免密sudo。用APT独立的用户级索引/缓存目录获取原配置中的七组源，包括noble及security/updates快照、porting、customization、bianbu-v2.2-updates和noble-ros。签名验证仍使用原厂商keyring，没有使用trusted、跳过校验或改写系统源。

为使只读审计可由普通用户执行，仅在审计配置清除APT更新后写系统缓存的hooks；系统APT配置和hooks没有改变。获取29.1MB索引，APT退出0，未出现源下载失败。目录为 `/home/bianbu/xt-stcar-tests/20260916-system-update`；系统 `/var/lib/apt/lists` 未更新。

| 检查 | 实际结果 |
| --- | --- |
| `apt-get -s upgrade`，使用新索引 | 0升级、0新增、0删除、0待升级保留 |
| `apt-get -s dist-upgrade`，仅模拟 | 0升级、0新增、1降级、1删除；未执行 |
| 完整升级拟删除 | 当前 `linux-image-6.6.63`，版本6.6.63-20250922112039 |
| 完整升级拟降级 | `bianbu-esos` 0.0.10 → 0.0.9 |
| `dpkg --audit` | 无未配置/损坏包报告 |
| `apt-mark showhold` | 空；但厂商APT pin仍存在，两者不同 |
| 重启提示文件 | 未发现 `/var/run/reboot-required`；本轮没有安装或重启 |
| Snap / Flatpak | 未安装对应命令 |

重点核对libc、OpenCV、ONNX Runtime、BlueZ、SSH与内核，当前APT候选均等于已安装版本。厂商对porting/customization/ROS设高优先级，并单独pin当前内核。不能把其他低优先级源存在的版本当作本系统应直接安装的更新，也不能直接执行full-upgrade/dist-upgrade：当前模拟结果会移除内核并降级包。

## 已有蓝牙异常

`realtek-bt.service` 在2026-09-15 18:38:35开机期间就失败，日志为 `bring up hci failed`；因此不是这次更新造成。当前 `/sys/class/bluetooth` 无HCI设备；rfkill显示蓝牙和Wi-Fi均未软/硬阻断。启动脚本属于bluez，当前源没有该包的新候选版本。

未重启服务、切换供电、刷固件或改驱动脚本。根因尚未确认，后续应单独诊断UART初始化、模块/固件和启动时序，不能以清除failed状态冒充修复。

完整命令输出和索引哈希见[机器验证记录](vehicle-system-update-validation.json)。本机原始日志位于 `work/vehicle-system-update/`。此次无系统包安装，最终实际状态为：工程新ORT可用、当前配套源无普通更新、原内核和libc保留、蓝牙启动异常未修复。
