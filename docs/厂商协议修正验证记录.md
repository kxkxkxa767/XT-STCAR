# 厂商协议修正验证记录

本轮 Rust 底盘编码与 IMU 流解析及回放集成完成；功能、命令与限制见 [适配说明](厂商协议Rust适配.md)。

- `bash scripts/build-riscv.sh --offline`：fmt、51 项常规 Rust 测试、clippy -D warnings、两个 release 交叉链接及 ELF 验证通过。1 项真实模型 opt-in 测试本次 ignored，不计入通过数量。
- 新增 6 项协议测试、3 项 CLI/整链路测试。18 项 Python 交付回归通过，包内新增配置和样例通过 allowlist、哈希验证。
- 原始 IMU 回放：9 输入事件、1 个完整样本、10 条运动记录，最终 fault。坏校验帧未刷新 IMU，80 ms 达到超时进入 Fault/Stop。
- 底盘预览 `teleop_pwm_degrees / 1500 / 90` 得到 `AA DC 05 DC 05 C2 55`，未打开串口。
- 交叉目标 `riscv64gc-unknown-linux-gnu.2.38`，两个程序实际最高 GLIBC 引用均为 2.34，符合 2.38 上限。
- xt-stcar：1213880 字节，SHA256 `0592ea68f687d8cd3a9609ace273e7722670e9d32d7074cd145e600cb4d303ff`。
- xt-stcar-robot：1359504 字节，SHA256 `a85281f669c12c8a86088b16c6a87831202cb534096e36e208588b5b2f5c261a`。
- 构建证据记录 27 个 Rust 源码/清单/锁文件；详见 factory-riscv-build.json、factory-riscv-elf.json、factory-robot-elf.json。
- 完整日志 factory-build.log、factory-delivery-tests.log、factory-imu-replay.jsonl；包校验 factory-delivery-validation.json。

没有修改厂商原始源码、读取视频、连接车辆或安装镜像。真实串口端口配置/限时 I/O、物理标定、ROS 适配和实车停车仍未实现或验收。
