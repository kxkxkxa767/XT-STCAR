# 车端固件更新检查

2026-09-17，用户要求检查车上固件是否可更新。SSH重新连接后完成只读核对、用户目录APT索引刷新及候选包解包审阅；没有安装固件、写启动分区或重启。

| 项目 | 已安装包版本 | 当前配套源结果 |
| --- | --- | --- |
| u-boot-spacemit | 2.2 | 存在2.2.7，但优先级500低于旧版1100，默认候选仍2.2 |
| opensbi-spacemit | 2.2 | 存在2.2.7，同样未成为默认候选 |
| spacemit-flash-dtbs | 1.1 | 存在1.2.3，为设备树刷写辅助工具，非独立固件 |
| k1x-vpu-firmware | 0.1.0 | 候选相同 |
| img-gpu-powervr | 24.2-6603887bb3 | 候选相同，包包含GPU固件 |
| bianbu-esos | 0.0.10 | 高优先级源候选为更旧0.0.9，不应降级 |
| wireless-regdb | 2022.06.06-0ubuntu2 | 候选相同，属于无线法规数据库 |

包版本不等于已烧录启动程序版本。本次内核日志显示SBI implementation version 0x10003，未从启动分区读取或确证U-Boot实际运行版本。系统仍为Bianbu2.2、定制PREEMPT_RT内核6.6.63-20250922112039。

## 新版启动包的实际行为

从现有签名APT源下载并仅解包审阅三个包，车端目录为 `~/xt-stcar-tests/20260917-firmware-audit`。明确指定三个新版的安装模拟结果为3升级、0新增、0删除；这只证明包依赖可解，不证明板级兼容。

当前根分区为/dev/mmcblk2p6。u-boot-spacemit的postinst会解除mmcblk2boot0只读并直接dd写bootinfo、FSBL、mmcblk2p2环境和mmcblk2p4 U-Boot；opensbi-spacemit会写mmcblk2p3。此操作涉及启动链且覆盖环境，不能仅凭版本号和依赖模拟执行。未确认车辆定制镜像与新版启动组件兼容，未建立启动分区备份和实测恢复路径，因此没有升级。

候选SHA256：

- opensbi-spacemit_2.2.7_all.deb：940600666a9f2427ca5d06bf6a67065a8df65d579bb4952758cff5704513438e
- u-boot-spacemit_2.2.7_all.deb：fd12c719f4f83be0fd55a930af746b7f242feb8ba5811fb069adf7d486932463
- spacemit-flash-dtbs_1.2.3_riscv64.deb：ff70d920b13e982bc5834865aa4f627f4d260274775922ed8fdd3b6c8e7a221f

## 其他渠道与边界

fwupd1.9.27识别eMMC及启动MTD分区，但get-updates报告无可用更新；LVFS元数据年龄24小时，本次未刷新fwupd元数据，因此不是对服务器最新目录的完整排除。UEFI capsule不可用。设备标记Updatable只说明机制支持，不代表有匹配固件。

[官方MUSE Pi Pro烧录说明](https://bianbu.spacemit.com/en/brdk/Quick_start/1.2_System_Flashing/1.2.1_MUSE_Pi_Pro/)提供Bianbu ROS整机镜像入口和FDL/Type-C恢复流程；它不等于本车定制系统的原位固件更新许可或兼容证明。本次未下载整机镜像。

蓝牙已经完成匹配RTL8852BS固件补齐，本次复核服务active、控制器Powered=yes；未确定有更新且兼容的蓝牙固件。底盘MCU、雷达、IMU及摄像头尚无已确认的现用固件版本与配套更新清单，不能宣称都是最新版；未读取或刷写MCU固件。结论为存在启动固件新版候选，但没有已完成兼容核验、适合直接执行的更新事务。
