# 车辆到场模块检查

2026-09-16。用户确认车辆到场、SSH已连接、车轮架空，授权测试各模块。本次通过用户认证的SSH复用会话执行，未取得或保存密码。下列结论只覆盖实际完成的项目。

## 实测结果

| 模块 | 实际检查 | 结论与限制 |
| --- | --- | --- |
| 主控与系统 | RISC-V，Bianbu 2.2，内核6.6.63 PREEMPT_RT，GLIBC2.39；内存7.7GiB，根分区约44GiB可用 | 原GLIBC2.38构建可运行；未更改系统库或系统服务 |
| 交付载荷 | 含模型包远端SHA-256一致，解包后SHA256SUMS全部通过，两程序动态依赖齐全 | 使用上一轮正式交付包，未重编译或覆盖厂商工作区 |
| Rust程序 | 视觉无模型self-check、机器人事件replay均退出0 | 是真实RISC-V执行，self-check仍是合成输入，不是YOLO推理 |
| N10雷达 | `/dev/laser → ttyACM0`，230400；Rust采集3秒，49,145字节，解码844包/13,504点，30次起始角回绕 | 校验、长度、角度、过期错误均0；距离0.549–5.197m。约10圈/秒只是本段观测；未验证测距精度、安装外参和去畸变 |
| WIT IMU | `/dev/imu → ttyUSB0`，115200；Rust采集3秒，9,903字节，299完整样本 | 校验错误和过期分量均0；加速度模长均值9.81875m/s²，角速度模长最大0.01076rad/s；未做零偏/安装姿态标定 |
| USB相机 | icSpring UVC，`/dev/video20`；OpenCV V4L2读取5帧640×480，检查实际图像可用 | 仅短时采集，未测持续帧率或完成相机内外参标定 |
| 系统ORT | 1.18.1，GetApi(22)返回空；Rust真实加载退出2并报告版本不兼容 | 该失败保留，不能靠无模型self-check掩盖 |
| 独立ORT与YOLO | 官方SpacemiT 2.0.6包内ORT1.24.2，原YOLO26n模型，Rust原生infer成功 | 相机图像检出人0.86995、杯子0.35725，命令报告1705.49ms。未启用EP，单次耗时不代表持续帧率 |
| Rust道路识别 | road-detect带同一模型/运行库处理真实相机图像，退出0 | 模拟标定在室内图像产生一个未验证锥桶候选；灯unknown、斑马线null。**仅入口执行通过，识别与定位准确性未验收** |
| 规划/任务 | 车端执行原`competition-sim.json`完整固定场，退出0、completed=true、757拍、模拟75.6秒，最终模拟速度0 | 首次90秒超时；扩大外部测试等待至300秒后，用时105.64秒完成，原算法/预算/场景不变。是合成闭环，不是实际行驶，亦不证明实时截止期 |
| 底盘架空响应 | `/dev/car → ttyUSB1`，38400；中性1500/1500，小幅转向1450/1550各0.4秒，油门1520/1540/1560各0.3秒且间隔回中性 | 用户现场确认舵机动作、电机明显转动、最后已停住；父进程/守护均补发停止，另完成中性收尾。后续已核对抽测档位的前进/左右方向，见下节；速度、转角及制动时延未标定 |

传感器输入解码仍使用示例零偏/frame配置（IMU为`sim_imu`），因此只是协议和基本数值检查，不是实际TF/融合验收。N10初始/末尾分包重同步丢弃193字节，IMU丢弃36字节，未将这些字节伪造成有效帧。各自采集有独立时间纪元，不能直接拼成同时刻传感器快照。

200ms规划冒烟也已执行，3拍132个候选，按人为场景时限故障并记录停止，退出2符合该截短实验用途；不算完整比赛通过。完整固定场的成功不更改此前在线正常整场0/15完成的结论。

## 底盘诊断与差异

车上`racecar_driver_node.cpp`与资料ZIP不同：先赋值`linear_x = twist->linear.x * 100 + 1500`，再比较已转换后的`linear_x > 0.09`并改用160系数。这不是对原始速度的阈值判断，本轮没有使用此映射换算m/s。

中性诊断使用车上源码确认的7字节帧和38400波特率，保持设备独占与有限写入等待，父进程与独立守护在结束/中断时补发中性帧。首次中性测试父进程停止写入15/20、守护20/20，诊断工具返回2；随后将父进程清理阶段的心跳持续到交接结束，复测双方20/20，首次异常日志保留。原因没有进一步单独证明，不将复测成功写成MCU看门狗已验证。

第一阶段用户确认中性测试无动作、转向测试“舵机动动了”，油门测试“转了，也比较明显”，并明确回答“转了，最后已停住”。据此将本轮舵机/电机基本响应与最终停止标为现场观察通过，仅凭第一阶段反馈不能推导每个脉冲都跨过死区或具体旋转方向，后续分档检查另列。

转向始终保持电机1500µs；油门始终保持舵机1500µs，三个0.3秒脉冲之间回到中性0.8秒，末尾中性保持并由父进程/独立守护各补20次停止。动作后又单独运行一次中性收尾，所有动作/复测退出0。仅中性情况下杀掉父进程的守护测试仍保留，不曾故意让带油门指令失去控制。

最新检查四个设备均无占用，测试子进程全部退出。停车依据是用户观察，当前没有MCU运动反馈传感器；原始程序日志的`physical_stop_observed=false`表示程序本身未观测，人工反馈另记录，不改写原件。应用层守护不覆盖主控断电、串口断线或MCU自身故障。当前仍需量测机械转向零点、速度/转角映射和制动时延，尚不具备落地自主运行验收证据。

## 架空方向与小油门分档复核

均为用户明确继续测试或指定数值后执行；转向保持电机1500，油门保持舵机1500，每轮结束补发中性帧。用户未看到的转向轮次单独保留记录，不算重复观察确认。

| 项目 | 指令与保持时间 | 用户反馈 |
| --- | --- | --- |
| 转向第一侧 | 1450µs，1.2秒 | 按车头朝向向右 |
| 中间回中 | 1500µs，1.2秒 | 回中；只是目视，机械零点未测量 |
| 转向第二侧 | 1550µs，1.2秒 | 按车头朝向向左 |
| 电机1520 | 0.5秒，随后1500 | 向前转，然后停止 |
| 电机1510 | 0.5秒，随后1500 | 没有转 |
| 电机1515 | 0.5秒，随后1500 | 仅回复“静止”；能确认结束状态，脉冲期间是否转动未确认 |
| 电机1519 | 用户指定，0.5秒，随后1500 | 没有转 |

每轮命令退出0，父进程与守护各20次中性补发成功；最后串口/相机无占用、测试进程已退出。1519未起转与1520起转是**本次单次架空、0.5秒脉冲**的观测差异，不能据此宣称阈值具备1µs精度、每次重复稳定、在地面负载下仍成立，或将1515补记成未起转。实际速度和制动时延无测量值，未修改生产控制参数或填写虚假速度标定表。

## 10秒保持补测

用户要求延长测试，并明确指定1520另测。保持架空、电机指令之外舵机固定1500，开始和结束均回中性；两个10秒测试及其后的中性收尾分别保存日志，未与前一轮并发执行。

| 电机指令 | 时长 | 现场反馈 | 能确认与不能确认 |
| --- | --- | --- | --- |
| 1519µs | 请求10秒，测试过程正常退出 | 从聊天回复到看到转动估计约20秒，间歇转了两次 | 确认长时保持下会出现转动；聊天时间含工具准备/传输/界面延迟，无法测定电机起动延迟 |
| 1520µs | 请求10秒，车端单调时钟记录指令阶段10.0195秒 | 报告开始转动，结束后明确确认“已经停住” | 确认起转和最终停止；未确认是否连续转满10秒，未测转速和制动时延 |

两个测试均退出0，父进程/守护各20次中性补发成功；1519反馈后另外补做中性收尾。最后检查`/dev/car`无占用，测试进程均退出。1520各阶段新增车端wall/monotonic时间，记录的是指令发送阶段，不是电机实测运动时间。

这次补测说明：0.5秒下1519未转而1520起转，**不能推出固定起转阈值为1520**。1519长时的间歇转动可作为后续死区、驱动响应或传动状态排查的现象，但当前没有电机电流、编码器或同步运动测量，不指定硬件故障原因、不用聊天延迟代替测速，也未把这些数值写进生产标定。

## 雷达与相机连续性追加检查

用户要求再次测试雷达和相机，本阶段未发送底盘指令。

- 雷达10秒采到163,509字节、2815个有效包、45,040个点槽；校验/长度/角度/过期错误均0，分包重同步丢弃239字节。
- 将原始字节在Mac交给当前生产Rust decoder与整圈组帧器，保持原95%覆盖/有效率、200ms圈时限、20ms包间隔限制，仅frame名称对应实际配置。得到99圈，拒绝0、push错误0；覆盖率99.17%–100%，每圈99–106ms。不是将二维点云生成算法改写成宽松测试。
- 已绘制一圈真实扫描供用户查看；图以雷达为原点，未标定到车体，不连接点形成虚构墙面，也不把缺回波区域当成空闲。
- 相机640×480与1920×1080质量检查分别成功读取125/65帧（各含5帧预热），无读取失败。两张实际图像可用，当前画面主要为近处白色板面；不能用此画面证明赛道识别或焦距/畸变已标定。
- 单独V4L2 MJPG传输各120帧，报告约29.8fps；去掉逐帧统计/哈希的OpenCV BGR读取各120帧，分别约14.91/9.93fps、失败0。带质量分析的循环更慢，不把分析开销误归为相机硬件降帧。BGR解码读取仍明显低于MJPG传输率，后续持续采集需优化这段路径；这不是YOLO推理帧率。

机器统计、源码锁文件与原始证据哈希见 [追加验证记录](vehicle-sensor-followup-validation.json)。没有测量距离真值、雷达外参、相机内外参或实时融合精度；视频文件未生成，原始图像和点云只保留本地。

## 相机8080网页预览

独立脚本 `scripts/camera-preview.py` 已复制到车端测试目录；依赖现有Python3/OpenCV，不需要Flask，不含底盘输出。绑定明确的内网IPv4地址，默认640×480、JPEG预览上限5fps，最多两个图像客户端。未配置开机自启。

在车端SSH终端执行，将示例地址替换成车的当前内网地址：

```bash
cd /home/bianbu/xt-stcar-tests/20260916-bringup
CAMERA_IP=你的车辆内网IPv4地址
nohup python3 camera-preview.py --device /dev/video20 --bind "$CAMERA_IP" --port 8080 > camera-preview.log 2>&1 &
echo $! > camera-preview.pid
```

同一局域网浏览器打开 `http://车辆内网IPv4地址:8080/`；当前单帧 `/snapshot.jpg`，连续视频 `/stream.mjpg`，状态 `/health`。
查看日志与停止（在上述目录执行；PID文件仅对应本次启动）：

```bash
tail -f camera-preview.log
# 退出日志查看用 Ctrl+C，停止服务另执行：
kill "$(cat camera-preview.pid)"
```

已实测状态返回480×640×3、error为null；单帧43,422字节且JPEG起止标记正常，连续流100,000字节内读到3个帧边界；SIGTERM结束退出0。测试后已停止服务。此测试只验证预览路径，不是持续吞吐或稳定性验收。预览期间会占用相机，执行其他相机采集前先停止预览；仅在可信局域网使用，不设公网转发。

## 位置与复现

- 车端独立目录：`/home/bianbu/xt-stcar-tests/20260916-bringup/`。
- `release/`为原含模型包；`spacemit-ort.riscv64.2.0.6/`为独立官方候选运行库；`results/`为本次数据。
- 厂商`/home/bianbu/racecar`只读核对，未启动其中控制节点或改写源码。
- 本机原始命令、脚本、stdout/stderr、原始传感器流和图片位于`work/vehicle-bringup/`，不进入Git；图片与现场进程/网络信息不公开。
- 可公开的统计和原始证据哈希见 [机器验证记录](vehicle-bringup-validation.json)。原包SHA与模型SHA同先前正式交付，不把包内历史“未接车”文档改写成新证据。

在车端测试目录复现无物理输出的检查：

```bash
cd /home/bianbu/xt-stcar-tests/20260916-bringup/release
bin/xt-stcar self-check --config config/yolo26n.json
bin/xt-stcar-robot replay --events examples/robot-sim.jsonl --config config/robot-sim.json
bin/xt-stcar infer --image ../results/camera.png --model models/yolo26n.onnx \
  --runtime-lib ../spacemit-ort.riscv64.2.0.6/lib/libonnxruntime.so.1.24.2+spacemit.a1 \
  --config config/yolo26n.json
bin/xt-stcar-robot autonomy-sim --config config/competition-sim.json
```

未执行落地自主行驶、MCU读取/刷写、系统libc替换、厂商视频读取或量化模型替换。ROS实时接口、全圈组帧/去畸变/ICP实测精度、联合时间轴、实际灯区/终点协议和车体运动标定仍待后续验证。


## 2026-09-17 落地短脉冲与暂停

用户将车放地面，提出用雷达估计行驶1米，并确认前方至少2米空地、现场可断电。随后分别明确要求1530、1540起步试探。两次均保持舵机1500，电机对应值仅0.3秒，随后回1500；父进程和独立守护各补发20次中性帧，退出0。用户分别反馈未移动，随后明确“先暂停”。立即停止后续运动测试，最后只读检查/dev/car、/dev/laser无占用。

运动前生产Rust雷达解码/整圈组帧得到19圈、拒绝0；1530/1540后各采集2秒雷达供后续分析。未完成扫描匹配、真实位移估计、制动距离标定或1米行驶，不能将短脉冲未起步当作固定PWM死区，也不能据本次记录宣称雷达测距精度优于相机。相机仅采集一张静态图，未做视觉里程计。

本机记录位于work/vehicle-ground，车端位于~/xt-stcar-tests/20260917-ground。当前按用户要求暂停；恢复运动需用户明确继续，不能沿用此前1米请求自动恢复。


### 用户恢复1550单次试探

用户随后明确要求1550保持1秒。本轮舵机1500，指令阶段实际约1.0046秒，结束回1500；父进程和守护各20次中性帧，正常退出。用户确认“动了，没有偏转，但速度很慢”，另确认最终停住。此授权恢复指定的单次试探，没有自动追加长时间油门。

前后静止扫描经生产解码/组帧，再用离线诊断trimmed point-to-point ICP匹配。距离过滤0.25–6米，各批按角度取中位数，最近邻上限0.22米并保留80%匹配；5组初始平移收敛到雷达原点位移约0.212米、旋转约0.62度，80%匹配RMSE约0.0163米。1530/1540两轮对应估计小于1毫米，只作为与人工未移动一致的现象，不能宣称亚毫米精度。详细结果见vehicle-ground-probe-validation.json，诊断源码和原始扫描保留work/vehicle-ground。

没有卷尺真值、车体外参、同步运动轨迹或相机视觉里程计对照，不能将雷达原点净位移视作准确车体行驶里程，不能从1秒油门与最终位移推导稳态速度。1米请求仍未完成；当前保持停车，不自动延长油门。


## 1米请求与1600短脉冲超距（2026-09-17，用户结束测试）

用户确认移走前方1米处的标定箱，前方至少2米畅通、有人看护并标记起点。初始雷达得到18圈，前向±25度51个角度槽中50个有效，最近有效回波1.853米；缺回波不算自由空间。使用此前受控试探方式，分段发PWM、回中性后再采集扫描，并非行驶中实时雷达距离闭环。

1. 1550、舵机1500、请求1秒，发油门到随后中性日志间隔1.0055秒；停车后雷达估计净位移约0.00016米，属于未检出明显移动，不宣称亚毫米真值。这与此前同档1秒起步约21厘米不同，说明起步/执行重复性未建立。
2. 用户要求试1600，明确执行0.3秒短脉冲、舵机1500；日志油门到中性间隔0.3020秒。父进程和独立守护各补20次中性帧、正常退出。
3. 原小范围ICP给出约0.083米，但10厘米内匹配比例只有31.9%、中位残差18.1厘米，因此明确拒绝该结果并停止追加动作。
4. 仅离线扩大初始平移搜索，最佳结果为净位移1.224米、旋转约1.97度，10厘米内匹配比例64.4%、中位残差4.32厘米、80%残差RMSE8.70厘米。质量仍不足以用作精确里程或自动控制，不能以低残差候选代替独立真值。
5. 用户反馈“大概1.25m，没有偏移，先到这吧，记得上传”。据此结束测试。**1米目标超出约25厘米，没有实现定距1米停车**；用户数字为近似观察，不编造精确卷尺读数或误差条。最后/dev/car与/dev/laser无进程占用。

这次表明，脉冲时长上限不等于位移上限；不能用1600的0.3秒和最终1.25米直接计算稳态速度，也不能认定1500就是主动制动。尚无同步运动过程或停止时延测量，无法区分加速、滑行与控制响应的贡献。今后恢复定距测试前应先建立低速重复性、停车/滑行距离和实时传感器停止链路，不能继续按时间比例追加油门逼近目标。

统计及日志校验见[本轮验证](vehicle-one-metre-attempt-validation.json)。原始扫描、动作日志与离线诊断脚本保留本机work/vehicle-ground及车端~/xt-stcar-tests/20260917-ground；没有上传现场图像和原始环境点云。当前用户要求结束，不自动继续运动或一米测试。


## 落地起步PWM：每档1秒与连续雷达（2026-09-17）

用户重新确认车回起点、至少2米空地及现场看护，要求测最低起步PWM和最高车速。仅执行低档起步边界测试；因1600/0.3秒曾超距、没有可靠实时停车链路及足够制动空间，未在室内2米通道测试最高车速。

| 次序/档位 | 保持时间 | 观察 |
| --- | --- | --- |
| 1510、1515、1520、1525、1530、1535、1540、1545 | 各1秒、各一次 | 前后雷达未检出明显净位移；不是证明动作期间完全不动 |
| 1550第一次 | 1秒 | 无明显净位移；用户观察车前倾一点，结束后回原位 |
| 1552 | 1秒 | 连续雷达估计净位移约0.392米；用户确认起步并在该轮确认已停住 |
| 1551 | 1秒 | 连续雷达估计净位移约0.337米；末段扫描稳定 |
| 1550复测 | 1秒 | 连续雷达估计净位移约0.180米；用户确认实际前进，末段扫描稳定 |

**本轮最低观察到起步的数值为1550，但没有找到可靠、固定的最低起步PWM。** 同一1550两次表现不同；1551和1552各成功一次，不能据此认定1551为稳定阈值或1微秒差异具备重复性。1510–1545没有在后续状态下再测，低于1510也未在本轮试验，不作全范围最小值证明。没有把这些观察写入正式PWM-速度映射。

前九轮使用动作前后2秒静态扫描，用户反馈1550短暂前倾后回原位，暴露只看净位移会漏掉瞬时动作的限制。随后三轮各采集8秒连续雷达，每轮生产解码/整圈组帧得到79圈、拒绝0；离线逐圈匹配用于诊断过程，**仍不是实时刹停控制**。1552/1551/1550复测的终态匹配80%残差RMSE约2.12/1.90/1.47厘米，1551有2圈未达诊断质量条件（匹配比例>80%、RMSE<4厘米），其余结果仍须独立真值校验。

对比进程启动时间与指令日志，1552在发中性后的扫描仍继续前移，约4.2秒采样位置才接近稳定；但采集的相对计时与Python进程启动并非硬件同步，整圈也有时间跨度，因此不能直接给出精确响应/制动时延。距离是雷达原点净位移估计，未校验卷尺真值，不能除以1秒当作稳态车速或最高速度；俯仰与场景变化也会影响二维匹配。

每轮均舵机1500，动作后回电机1500，父进程和独立守护各补20次中性帧，正常退出；最终car/laser无占用，未再加档。统计及文件哈希见[起步PWM验证](vehicle-startup-pwm-validation.json)，原始扫描和诊断代码保留work/vehicle-ground。后续应在统一地面/负载/供电条件下研究重复性及停车过程，而不是继续追高PWM。
