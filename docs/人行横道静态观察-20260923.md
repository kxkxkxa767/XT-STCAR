# 2026-09-23 人行横道静态观察与手动到线记录

## 最新状态

用户在发车区静止后，明确目标为开到斑马线前停车、不压白条。随后改由用户手动驾驶到白线处，并回复“好了”。这是用户手动到位，不是自动识别停车验收；车头到最近白条的物理距离尚未尺量，不能断言精确停车位置。

助手本轮仅通过已有驾驶台 GET 接口读取状态、视觉结果和单张相机画面；未发送 arm/drive/stop，未写串口、重启服务、部署模型或启动训练。

## 只读核对

- 发车区及用户到位后的两次采样均 healthy=true、armed=false，motor=1500、servo=1500，停因 operator_stop；这仅是采样时的软件状态，不是独立轮速测量。
- 到位后设置为 forward=1550、reverse=1350、left=1650、right=1350。与旧交接所列前进1570不同；本轮未修改设置，后续以实时值为准。
- 模型类别仍为单类 ["blue_cone"]，两次采样 detections=[]、crosswalk=null、observations=[]。calibration_status=unverified、simulation_only=true。
- 发车区采样视觉 total_ms≈840、age_ms≈1114；到位采样 total_ms≈822、age_ms≈1462。是各自单帧读数，不是帧率或最坏延迟验收。相机JPG和视觉结果分别读取，并非同帧同步证据。
- 发车区图像中远处白条很小；用户到位后的图像可见前方一排白条。之前近景已做人工像素轮廓标注（4条完整、右侧1条截断），不是自动检测结果，也不能据可见条数认定完整场地条数。

## 角度适应方案：尚未实施

源码 road.rs 已有 stripe_axis=either，并非完全不支持旋转；现有方向、长宽比和间距筛选仍可能受透视压缩影响。漏检具体首因尚未逐阶段复现，不能断言某一个阈值就是原因。

候选方案为：分离图像识别与米制测距，利用局部对比度/边缘提取条纹四边形，按共同透视关系分组，允许随透视变化的尺寸和间距，再用多帧一致性抑制反光与白墙误报。队友继续负责新四类模型训练，可补充真实多视角素材。摄像头安装角变化后地面投影需更新，不能沿用旧矩阵直接控制停车。

本轮没有修改检测算法、配置、地面标定或停车控制。自动从发车区到线前停车尚未实现/验证，不能将人工标注或用户手动驾驶计为自动通过。

## 本机证据

原图和标注保存在本次 Codex 任务 outputs/，状态快照在 work/crosswalk-check/；项目忽略目录 work/crosswalk-static-20260923/ 保存一份副本。图像及人工标注不作为代码测试通过证据，本轮不上传现场照片。
